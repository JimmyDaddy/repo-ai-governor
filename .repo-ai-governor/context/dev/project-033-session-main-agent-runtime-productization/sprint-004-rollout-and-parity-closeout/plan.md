# sprint-004-rollout-and-parity-closeout 计划

- Status: completed
- Date: 2026-03-31
- Project: `project-033-session-main-agent-runtime-productization`
- Sprint Goal: 收口 CLI session shell / resume / desktop consumer parity 验证，并沉淀 path-A productization 的最终 rollout evidence。

## 1. Task Package

1. `TK-457` verify CLI session shell resume and desktop consumer parity for real main-agent turns
2. `TK-458` close docs review and rollout evidence for path-A productization

## 2. Exit Criteria

1. CLI session shell 与 resume path 对真实 `session.main` turn 的展示语义保持一致。
2. desktop consumer baseline 至少对同一份 shared session truth 保持 contract-level parity 说明或验证记录。
3. project-033 的 docs / review / rollout evidence 与 `current-context.md` planned stream surface 保持同步。
4. sprint-004 只承接 parity / rollout closeout，不重新打开已完成的 `TK-451 ~ TK-456` 范围。

## 3. Milestones

1. 2026-03-31：基于已完成的 `sprint-001 ~ sprint-003` 与已收口的 working-tree CR，创建 `sprint-004` closeout planning surface。
2. 2026-03-31：冻结 `TK-457 ~ TK-458` 作为 path-A 的最终 rollout / parity follow-up task package。
3. 2026-03-31：激活 `sprint-004` 并开始执行 `TK-457`，收口 CLI session shell、resume 与 desktop consumer baseline 的 parity 验证。
4. 2026-03-31：完成真实 `session.main` parity integration regression、resolved review、DA closeout 与 project completion audit；`sprint-004` 收口为 completed truth，并暂保留为 active closeout surface 直至下一条主执行流显式激活。
