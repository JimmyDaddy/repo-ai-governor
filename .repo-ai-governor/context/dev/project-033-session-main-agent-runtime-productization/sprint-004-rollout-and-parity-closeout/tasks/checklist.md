# checklist

- [x] TK-457 verify CLI session shell resume and desktop consumer parity for real main-agent turns
  - 2026-03-31：任务创建，状态初始化为 `planned`；目标是验证 CLI session shell、resume flow 与 desktop consumer baseline 对真实 main-agent turns 的语义一致性。
  - 2026-03-31：主执行流已切换到 `project-033 / sprint-004`；开始核对 CLI session shell、resume 与 desktop consumer baseline 的 parity seam。
  - 2026-03-31：已新增 `session-main-parity.integration.test.ts`，通过真实 `session.main` payload / CLI resume parity 回归；本任务完成。
- [x] TK-458 close docs review and rollout evidence for path-A productization
  - 2026-03-31：任务创建，状态初始化为 `planned`；目标是补齐 docs/review/rollout evidence，并为 project-033 的最终 closeout 做准备。
  - 2026-03-31：已补齐 resolved review、DA closeout 与 project completion audit summary，并同步 project / sprint / current-context closeout 状态；本任务完成。
