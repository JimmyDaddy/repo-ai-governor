# checklist

- [x] TK-451 activate project-033 and sync path-A phase map
  - 2026-03-31：任务创建，状态初始化为 `planned`；目标是建立 `project-033` 的 plan/sprint/task surface，并将路径 A follow-up 登记到 `current-context.md`。
  - 2026-03-31：已完成 `project-033` planning surface、`current-context` planned stream 登记与 path-A phase map 对齐。
- [x] TK-452 formalize session.main contract delta and structured turn semantics
  - 2026-03-31：任务创建，状态初始化为 `planned`；目标是把 `session.main` 从 `baseline_ack` 升级到真实主 agent turn 所需的 contract delta、event semantics 与 structured result shape 写成正式执行输入。
  - 2026-03-31：已将 `session.main contract delta`、责任边界与推荐实现顺序写回 session-first shell draft，作为 `TK-453 ~ TK-456` 的正式输入。
