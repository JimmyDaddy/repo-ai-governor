# sprint-001-agent-protocol-and-adapter-sdk 计划

- Status: active
- Date: 2026-03-21
- Project: `project-004-agent-adapter-runtime`

## 1. Sprint Goal

完成 Role Registry、Agent 协议与 Adapter SDK 的最小可用契约，并形成 sprint-002 输入约束。

## 2. In-Scope Tasks

1. TK-032 Role Registry 与 Role Profile 生命周期基线（completed）
2. TK-033 Agent 协议与 Capability Matrix 基线（planned）
3. TK-034 Adapter SDK 与 routeKey 主备路由基线（planned）
4. TK-035 sprint-001 出口验收与 sprint-002 输入约束（planned）

## 3. Entry Criteria

1. `DA-039`（project-003 出口验收基线）与 `DA-040`（project-004 输入约束清单）可检索。
2. Stage 4 产物（Standards/Slot/Spec Sync）可作为 Role 与 Adapter 语义的约束输入。

## 4. Exit Criteria

1. Role Registry 支持默认角色与用户自定义角色生命周期治理。
2. Agent 协议与 capability matrix 可被 Adapter SDK 稳定消费。
3. `routeKey` 主备路由与降级回退语义可验证。
4. 形成 sprint-001 验收基线与 sprint-002 输入约束清单。
