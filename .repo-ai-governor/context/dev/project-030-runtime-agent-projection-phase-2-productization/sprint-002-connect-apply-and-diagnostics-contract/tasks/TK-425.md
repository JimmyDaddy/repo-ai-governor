# TK-425 emit candidate diff and merge explain artifacts

- Status: completed
- Date: 2026-03-30
- Owner: AI-Agent
- Priority: P0
- Project: `project-030-runtime-agent-projection-phase-2-productization`
- Sprint: `sprint-002-connect-apply-and-diagnostics-contract`

## 1. 目标

为 `connect` 与 `connect diff` 输出 candidate diff JSON/Markdown 和 merge explain artifacts，让用户一眼看清 role/tool/routing 将发生的变化以及 merge/overwrite 语义。

## 2. Acceptance

1. `connect` 默认生成 diff / merge explain companion artifacts。
2. diff artifacts 同时包含 machine-readable 与 human-readable 视图。
3. merge explain 明确说明 untouched / replaced / removed / fallback-preserving 路由语义。
4. 相关 diagnostics contract 与 tests 同步通过。

## 3. Execution Notes

1. 2026-03-30：任务创建，待在 connect candidate diagnostics 基线之上补 diff / merge explain companion artifacts。
2. 2026-03-30：完成 candidate diff JSON/Markdown、merge explain artifacts、apply-ready blocker/risk notes contract，并在 `connect` 与 `connect diff` 上统一复用。
