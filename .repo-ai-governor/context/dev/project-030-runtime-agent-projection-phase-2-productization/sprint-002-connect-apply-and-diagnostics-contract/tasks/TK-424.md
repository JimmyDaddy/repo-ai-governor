# TK-424 implement connect diff, apply, and rollback receipt workflow

- Status: completed
- Date: 2026-03-30
- Owner: AI-Agent
- Priority: P0
- Project: `project-030-runtime-agent-projection-phase-2-productization`
- Sprint: `sprint-002-connect-apply-and-diagnostics-contract`

## 1. 目标

为 `connect` 增加 `diff/apply` 子面、source fingerprint 校验、rollback snapshot 与 apply receipt，使 candidate config 从“仅可生成”演进为“可审阅、可显式应用、可回滚”。

## 2. Acceptance

1. `connect diff` 支持显式 candidate path 或 `--latest`。
2. `connect apply` 会校验 candidate/source fingerprint，并在写回前生成 rollback snapshot。
3. apply 成功后会写出 receipt artifact，并给出后续 `doctor/verify/run --dry-run --trace` 提示。
4. JSON / pretty output 与 tests 同步通过。

## 3. Execution Notes

1. 2026-03-30：任务创建并激活，待补 `connect diff` / `connect apply` 入口、runtime、artifacts 与 output contract。
2. 2026-03-30：完成 `connect diff` / `connect apply`、`--latest` / candidate path 入口解析、source fingerprint guard、rollback snapshot、apply receipt 与 CLI integration coverage。
