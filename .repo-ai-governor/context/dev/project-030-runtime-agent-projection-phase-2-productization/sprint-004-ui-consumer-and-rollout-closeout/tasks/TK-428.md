# TK-428 add formal UI consumer baseline for desktop and richer UI surfaces

- Status: completed
- Date: 2026-03-30
- Owner: AI-Agent
- Priority: P0
- Project: `project-030-runtime-agent-projection-phase-2-productization`
- Sprint: `sprint-004-ui-consumer-and-rollout-closeout`

## 1. 目标

新增一个 transport-neutral `agent projection` view-model + React panel seam，并让现有正式 UI surface 首次消费它，作为 desktop / richer UI 的正式 consumer baseline。

## 2. Acceptance

1. 存在一个可复用的 `AgentProjectionPanelViewModel` builder，输入为 shared `agentView` DTO，输出为 transport-neutral panel model。
2. 存在一个 React panel 组件消费该 view-model，而不是继续把 `agentView` 压平为纯字符串列表。
3. 至少一个正式 UI consumer 已接入该 panel；优先接入现有 command-level React shell，并保证后续 desktop surface 可复用同一 seam。
4. 相关 tests、build evidence 与 desktop baseline docs/sample 保持同步。

## 3. Execution Notes

1. 2026-03-30：任务创建并在 `project-030 / sprint-004` 激活时转为 `active`。
2. 2026-03-30：新增 `CliAgentProjectionPanelViewModel` transport-neutral seam、`CliAgentProjectionPanelViewModelBuilder` 与 `ReactCliAgentProjectionPanel`，并让 `connect` command-level React shell 成为首个正式 consumer。
3. 2026-03-30：同步更新 `integrations/desktop/**` baseline docs，使 desktop / richer UI 明确回链同一 panel seam，而不是旁路 runtime internals。
