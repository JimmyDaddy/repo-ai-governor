# TK-427 enrich agentView presenter in pretty and session-shell surfaces

- Status: completed
- Date: 2026-03-30
- Owner: AI-Agent
- Priority: P0
- Project: `project-030-runtime-agent-projection-phase-2-productization`
- Sprint: `sprint-003-smoke-gate-and-agent-view-presentation`

## 1. 目标

补共享 `agentView` presenter，并升级 CLI pretty 与 session-shell nested summary 渲染，让 `selected_by`、projection status、fallback reason 与 capability gap 变得直观。

## 2. Acceptance

1. 存在一个可复用的 shared presenter / view-model seam。
2. pretty output 至少展示 selection reason、projection status 与 capability gap summary。
3. session-shell nested command summary 能消费同一套 presenter 结果，而不是重复拼字符串。

## 3. Execution Notes

1. 2026-03-30：任务创建，等待 `sprint-002` 的 diff/apply artifacts 与 connect diagnostics contract 稳定后执行。
2. 2026-03-30：完成 shared `CliAgentProjectionPresenter`，并将 fallback / capability-gap / selected_by highlights 接入 CLI pretty output 与 session-shell nested summary。
