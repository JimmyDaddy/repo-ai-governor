# TK-439 switch default foreground input owner from readline to Ink

- Status: completed
- Date: 2026-03-30
- Owner: AI-Agent
- Priority: P0
- Project: `project-031-session-shell-ink-input-productization`
- Sprint: `sprint-004-default-cutover-and-rollout-closeout`

## 1. 目标

把 default session-shell TTY path 的 foreground input owner 正式切换到 Ink。

## 2. Acceptance

1. local TTY + `pretty` + no subcommand 的默认 session shell 使用 Ink foreground input owner。
2. `readline` 仅保留 fallback / multiline seam。

## 3. Execution Notes

1. 2026-03-30：`CliSessionShellRunner` 默认优先消费 Ink action stream。
2. 2026-03-30：clean temp repo TTY smoke 已验证 `/` 即时打开 palette、`Tab` completion 与 `stderr-only`。
3. 2026-03-30：根据手工安装反馈补修了 submit 后 remount 造成的 append-only 视觉回退；现在普通 slash submit 会保持同一 Ink tree 持续挂载，仅 `/multiline` 仍临时让出给 prompt adapter。
4. 2026-03-30：composer 改成强调型 prompt box，slash palette 改成紧贴输入框的 bordered dropdown；clean temp repo TTY smoke 已验证 `/workspace` 的 palette 与 preview 转场会原位刷新。
5. 2026-03-30：将 Prompt Bar 进一步压缩为单行状态栏，只保留 mode / route / session 简写与上下文相关快捷键，降低对 composer 与 palette 的视觉干扰。
6. 2026-03-30：`/help` 调整为前台 palette 视图而不是 transcript 持久化事件；连续触发帮助只会刷新当前命令面板，不再把 transcript 区域重复拉长。
7. 2026-03-30：进一步把 bare `/` 收敛为 launcher shortlist，不再默认摊开全量命令；同时移除了 `query=/` 调试行、压缩了 palette 摘要行，并把空态 placeholder 改成更轻的 idle hint。
8. 2026-03-30：把默认首屏里的 session/runtime 调试信息收起，只保留标题、输入区与轻量快捷键提示；新增 `/status` 作为显式诊断入口，并抑制 startup transcript 的 service-backed session 元数据回放。
9. 2026-03-30：真实 PTY smoke 已验证默认首屏不再展示 `session_id=` / `shell_mode=` 等调试信息，且 `/status` 会按需展开隐藏运行时详情。
10. 2026-03-30：把 `?` 做成真正的 shortcuts/help alias；默认空态文案与状态栏已同步提示，真实 PTY smoke 已验证输入 `?` 会即时打开完整帮助 palette。
11. 2026-03-30：修复 Slash Palette 中上下选择后的 `Enter` 语义；现在第一次 `Enter` 会先接收高亮命令，只有完整命令或带参数输入才会继续走提交执行。
12. 2026-03-30：修复 Ink live tree 挂载期间仍落回旧的 stderr string renderer 造成的边框残影叠加；command execution 期间现在统一复用活动 Ink rerender，不再混用两套渲染通道。
13. 2026-03-30：继续根据手工体验反馈，把历史区与输入区做成弱历史、强输入的层级；标题收口为 `会话记录 / 当前输入`（英文 `History / Current input`），同时弱化 transcript 标题、强化输入框标题与分隔线。
14. 2026-03-30：定位到命令选择时的明显闪动来自 `CliSessionShellInkRunner.render()` 在每次 rerender 前都执行 `clear()`；已改成仅在显式清屏请求（如 `/clear` / `Ctrl+L`）后才在下一帧执行 viewport clear，避免 palette 高亮移动时整屏闪烁。
15. 2026-03-30：根据手工验证继续收口残影回归；在显式清屏之外，再为 live Ink rerender 引入布局签名判定，只有 frame 结构发生变化时才执行 viewport clear，避免命令提交后的边框残留，同时继续抑制 palette 上下选择时的闪动。
