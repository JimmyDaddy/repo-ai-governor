# TK-434 unify composer palette and handoff preview state under controller actions

- Status: completed
- Date: 2026-03-30
- Owner: AI-Agent
- Priority: P0
- Project: `project-031-session-shell-ink-input-productization`
- Sprint: `sprint-002-action-driven-runner-and-palette-state`

## 1. 目标

把 composer、slash palette、handoff preview 与 prompt-bar 的 presenter-local 状态统一到显式 controller/action 模型。

## 2. Acceptance

1. live 输入时 `shell_mode / input_mode / query / highlighted_command` 同步更新。
2. 非 slash 态不再误触发 palette completion。
3. prompt-bar 在 live 输入过程中保持与当前 foreground state 一致。

## 3. Execution Notes

1. 2026-03-30：补齐 `CliSessionShellInkController` 的 palette interactive guard，避免空 composer 误触发 `Tab / Up / Down`。
2. 2026-03-30：runner 在每个 action 后重新构建 prompt-bar lines，确保 live shell surface 与 state machine 同步。
