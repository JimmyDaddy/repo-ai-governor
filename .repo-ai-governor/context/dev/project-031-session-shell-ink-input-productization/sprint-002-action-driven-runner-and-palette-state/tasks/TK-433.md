# TK-433 refactor session-shell runner to consume action-driven input stream

- Status: completed
- Date: 2026-03-30
- Owner: AI-Agent
- Priority: P0
- Project: `project-031-session-shell-ink-input-productization`
- Sprint: `sprint-002-action-driven-runner-and-palette-state`

## 1. 目标

把 `CliSessionShellRunner` 从阻塞式 `readLine()` 循环演进为默认消费 Ink action stream 的 runtime。

## 2. Acceptance

1. live TTY 路径优先走 Ink action stream。
2. 退出、提交与 slash command 仍保持既有 session-shell contract。
3. fallback seam 继续可用。

## 3. Execution Notes

1. 2026-03-30：新增 `runWithInkInput()`，让 runner 直接消费 `CliSessionShellInkRunner.readAction()`。
2. 2026-03-30：完成 `submitComposerValue()` bridge，使 plain text / slash / passthrough / multiline 仍沿用统一提交语义。
