# sprint-001-p1-five-gap-closure 计划

- Status: completed
- Date: 2026-04-05
- Project: `project-046-p1-product-surface-and-delivery-closure`
- Sprint Goal: 完成 backlog 指定的五个 `P1` 缺口，并补齐 code/docs/config/template/evidence 的验证闭环。

## 1. Task Package

1. `TK-551` close desktop artifact pane query contract and typed renderer consumption
2. `TK-552` strengthen formal adapter support matrix and local-model positioning evidence
3. `TK-553` land standards runtime loader and governor config assembly contract
4. `TK-554` publish official GitLab CI and Jenkins templates for P1 baseline
5. `TK-555` close GA readiness signal #1 with normalized onboarding timing evidence

## 2. Exit Criteria

1. Desktop artifact/review/transcript seam 已进入 `apps/desktop` 的 typed view-model 与 service runtime surface。
2. 支持矩阵与 GA readiness 证据已从“条件化/待补指标”收口为明确正式口径。
3. Standards runtime loader 与 `governor.yaml.standards` contract 已有测试与文档闭环。
4. GitLab / Jenkins 模板资产已发布并写入正式文档。
5. build、测试、governance checks、review、completion audit 已齐备。

## 3. Milestones

1. 2026-04-05：创建 `sprint-001-p1-five-gap-closure`，承接 backlog 中五个指定 `P1` 任务的统一执行窗口。
2. 2026-04-05：五个 P1 任务全部完成，desktop artifact pane、adapter support truth、standards runtime loader、GitLab/Jenkins 模板与 GA onboarding timing evidence 已同步收口。
