# sprint-001-codex-claude-host-ergonomics-lifecycle-and-upgrade 计划

- Status: completed
- Date: 2026-04-08
- Project: `project-067-host-plugin-skill-agent-lifecycle-and-adopter-consumption`
- Sprint Goal: 为 Codex / Claude Code plugin / skill / agent 资产冻结 lifecycle、upgrade、support-truth 与 adopter-consumption contract。

## 1. Task Package

1. `TK-679` freeze codex claude host asset lifecycle and support-truth contract
2. `TK-680` implement codex claude host asset apply verify upgrade and adopter-consumption follow-up
3. `TK-681` close codex claude host ergonomics follow-up with README support-matrix playbook and packaging evidence refresh
4. `TK-702` sprint-001 exit acceptance and project-final review activation handoff
5. `TK-703` finalize project-067 closeout and activate project-064 primary stream

## 2. Exit Criteria

1. host asset lifecycle / support-truth contract 已冻结。
2. apply / verify / upgrade follow-up 已具备最小闭环。
3. README / support matrix / playbook / evidence 已形成一致 narrative。
4. sprint-level clean closure 已写回，并将当前 sprint surface 固定为 project-final delegated CR loop 的默认面。
5. project-final clean closure 已完成，并已把主执行流切换到 `project-064 / sprint-001 / TK-670`。

## 3. Milestones

1. 2026-04-08：作为 `project-067` follow-up sprint 创建，当前保持 `planned`。
2. 2026-04-08：在 `project-063` final closeout 完成后被激活为当前 primary sprint，`TK-679` 已切换为 `in_progress`。
3. 2026-04-08：`TK-679`、`TK-680`、`TK-681` 已全部完成实现与 evidence write-back，当前进入 sprint CR loop 前置窗口。
4. 2026-04-08：`CR-004` clean `resolved`；`TK-702 / DA-702` 已完成 sprint closeout handoff，当前 sprint surface 继续保持 `active`，但仅供 `project-067` project-final CR loop 使用。
5. 2026-04-08：`CR-005` clean `resolved`；`TK-703 / DA-703` 已完成 project-final closeout write-back，当前 sprint 已恢复为最终 `completed` 真值并移入 completed history。
