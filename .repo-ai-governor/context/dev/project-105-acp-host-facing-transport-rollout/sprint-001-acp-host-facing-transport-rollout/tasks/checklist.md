# checklist

- [x] TK-860 implement ACP host-facing transport rollout baseline
  - 2026-04-13：任务创建，状态初始化为 `planned`，作为 `followup_required` rollout skeleton 的 canonical task。
  - 2026-04-15：`project-104` final closeout 完成后，当前任务切换为 `in_progress`，并作为 `project-105 / sprint-001` 的 implementation 入口；下一步先本地预留 `CR-001`，再开始 `acp_exec` distinct transport 与 `acp_host_companion` carrier 实现。
  - 2026-04-15：已将 `acp_exec` 正式加入 shared transport truth，并在 CLI runtime 落下 fail-closed ACP host protocol baseline：routing 会显式返回 `acp_exec` protocol、local probe 不再把 ACP 误当 `cli_exec` 命令检查、projection 会保留 `acp_host_companion`、continuation seam 会对 ACP 直接返回 `null`。同窗 focused suites、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已通过，当前 baseline implementation 完成并进入 `CR-001` review loop。
- [x] TK-882 implement explicit acp_exec transport routing and fail-closed separation from cli_exec
  - 2026-04-14：任务创建，状态初始化为 `planned`。
  - 2026-04-15：已将 `acp_exec` 作为显式 transport 纳入 routing/runtime truth，并通过 CLI-local synthetic ACP protocol 保持 fail-closed 分离：`createProtocolBySurface()` 在 `acp_exec` 下不再复用 `cli_exec` adapter，verification/local probe 不再要求 `codex` 命令存在，health check 会稳定保留 `transportKind=acp_exec` 与 ACP baseline diagnostics，避免 same-surface transport rewrite。focused routing suites、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已在同窗通过，当前任务完成。
- [x] TK-883 project acp_host_companion carrier without polluting session or continuation canonical truth
  - 2026-04-14：任务创建，状态初始化为 `planned`。
  - 2026-04-15：已将 `acp_host_companion` 正式接入 `AgentDescriptor` additive carrier，并在 projection runtime 中为 `acp_exec` 提供稳定的 `hostReadinessStatus / distributionBoundary / companionStateSummary` companion；同时 provider continuation seam 对 `acp_exec` fail-closed 返回 `null`，确保 ACP-local truth 不污染 shared session / continuation canonical state。focused projection suites、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已通过，当前任务完成。
- [x] TK-884 sprint-001 exit acceptance and sprint-002 activation handoff
  - 2026-04-14：任务创建，状态初始化为 `planned`。
  - 2026-04-15：`TK-860`、`TK-882`、`TK-883` 与本地 `CR-001 ~ CR-009` 已全部 clean 收口；当前已将 `sprint-001` 标记为 `completed`，并把 `sprint-002` 激活为新的 primary execution surface。
  - 2026-04-15：`TK-885` 已切换为 `in_progress` 作为 `sprint-002` implementation 入口；下一步在 active sprint surface 预留本地 `CR-001` 后进入 ACP readiness composition 与 runtime-service enablement rollout。
  - 2026-04-15: Sprint-001 closeout completed; sprint-002 is active and ready for local CR reservation plus implementation.
- [x] CR-001 sprint-001-acp-host-facing-transport-rollout delegated review loop round 1
  - 2026-04-15：任务创建，状态初始化为 `review_pending`。
  - 2026-04-15：fresh reviewer round 1 返回 1 条 actionable finding；主 agent 复核后判定为 `accepted`，指出 `CliAcpHostProtocol#createUnavailableError()` 直接输出英文 fail-closed RuntimeError，违反 `CS-033` 的用户可见文本 i18n 基线。
  - 2026-04-15：accepted finding 已修复；当前通过 `CliAdapterRoutingRuntime` 向 ACP protocol 注入 `localizeText` bridge，并补上 `acp_exec` fail-closed 错误本地化断言，同窗 `pnpm exec vitest run apps/cli/test/runtime/adapter-routing-runtime.test.ts apps/cli/test/runtime/agent-onboarding-runtime.test.ts apps/cli/test/runtime/agent-projection-runtime.test.ts apps/cli/test/runtime/adapter-verification-runtime.test.ts apps/cli/test/runtime/session-main-provider-continuation-runtime.test.ts packages/config/test/config.unit.test.ts packages/core-agent-projection/test/agent-projection-service.unit.test.ts`、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已通过，当前 round 收口为 `resolved`。
- [x] CR-002 sprint-001-acp-host-facing-transport-rollout delegated recheck loop round 2
  - 2026-04-15：任务创建，状态初始化为 `review_pending`。
  - 2026-04-15：fresh reviewer round 2 返回 1 条 actionable finding；主 agent 复核后判定为 `accepted`，指出 `acp_exec` probe path 没有保留 config-derived unavailable truth，disabled/config-blocked surface 会被误报成 `acp_host_transport_not_ready`。
  - 2026-04-15：accepted finding 已修复；当前 `CliAdapterRoutingRuntime` 会把 `availabilityStatus / unavailableReasons` 透传到 ACP protocol，probe resolution 在 config-blocked 场景下优先保留配置真值，并补上 disabled `acp_exec` surface 回归断言。同窗 `pnpm exec vitest run apps/cli/test/runtime/adapter-routing-runtime.test.ts apps/cli/test/runtime/adapter-verification-runtime.test.ts apps/cli/test/runtime/agent-onboarding-runtime.test.ts apps/cli/test/runtime/agent-projection-runtime.test.ts apps/cli/test/runtime/session-main-provider-continuation-runtime.test.ts packages/config/test/config.unit.test.ts packages/core-agent-projection/test/agent-projection-service.unit.test.ts`、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已通过，当前 round 收口为 `resolved`。
- [x] CR-003 sprint-001-acp-host-facing-transport-rollout delegated recheck loop round 3
  - 2026-04-15：任务创建，状态初始化为 `review_pending`。
  - 2026-04-15：fresh reviewer round 3 返回 1 条 actionable finding；主 agent 复核后判定为 `accepted`，指出 `acp_exec` onboarding payload 把 `provider_kind / vendor_binding_kind / model` 置为 `null`，与现有 onboarding contract 只允许 `baseline / cli_exec` row 置空 provider fields 的约束冲突。
  - 2026-04-15：accepted finding 已修复；当前 `CliAgentOnboardingRuntime` 会在 `transport_kind=acp_exec` 时继续透传 configured remote-api provider / vendorBinding / model，相关 ACP onboarding test expectation 也已更新。同窗 `pnpm exec vitest run apps/cli/test/runtime/agent-onboarding-runtime.test.ts apps/cli/test/runtime/adapter-routing-runtime.test.ts apps/cli/test/runtime/adapter-verification-runtime.test.ts apps/cli/test/runtime/agent-projection-runtime.test.ts apps/cli/test/runtime/session-main-provider-continuation-runtime.test.ts packages/config/test/config.unit.test.ts packages/core-agent-projection/test/agent-projection-service.unit.test.ts`、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已通过，当前 round 收口为 `resolved`。
- [x] CR-004 sprint-001-acp-host-facing-transport-rollout delegated recheck loop round 4
  - 2026-04-15：任务创建，状态初始化为 `review_pending`。
  - 2026-04-15：fresh reviewer round 4 返回 1 条 actionable finding；主 agent 复核后判定为 `accepted`，指出 `acp_exec` onboarding row 继续把 `credential_mode / endpoint_source` 投影为 `null`，与 onboarding contract 要求保留 transport-aware selector truth 的约束冲突。
  - 2026-04-15：accepted finding 已修复；当前 `CliAgentOnboardingRuntime` 会在 `transport_kind=acp_exec` 时复用 ACP disclosure guardrail，继续透传 configured remote-api selector truth，相关 ACP onboarding test expectation 也已更新。同窗 `pnpm exec vitest run apps/cli/test/runtime/agent-onboarding-runtime.test.ts apps/cli/test/runtime/adapter-routing-runtime.test.ts apps/cli/test/runtime/adapter-verification-runtime.test.ts apps/cli/test/runtime/agent-projection-runtime.test.ts apps/cli/test/runtime/session-main-provider-continuation-runtime.test.ts packages/config/test/config.unit.test.ts packages/core-agent-projection/test/agent-projection-service.unit.test.ts`、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已通过，当前 round 收口为 `resolved`。
- [x] CR-005 sprint-001-acp-host-facing-transport-rollout delegated review loop round 5
  - 2026-04-15：任务创建，状态初始化为 `review_pending`。
  - 2026-04-15：fresh reviewer round 5 返回 1 条 actionable finding；主 agent 复核后判定为 `accepted`，指出同一 lane 从 `remote_api / cli_exec` 切到 `acp_exec` 时，旧的 provider continuation slot 没有被主动清理。
  - 2026-04-15：accepted finding 已修复；当前 `SessionMainProviderContinuationRuntime` 会在 continuation-unsupported transport 下继续检测同 lane 旧 slot，并发出 pre-dispatch clear mutation，`SessionMainSupervisorRuntime` 也只在确实存在 continuation request 时才下发 request 字段。新增回归测试覆盖 `remote_api -> acp_exec` invalidation path；同窗 `pnpm exec vitest run apps/cli/test/runtime/agent-onboarding-runtime.test.ts apps/cli/test/runtime/adapter-routing-runtime.test.ts apps/cli/test/runtime/adapter-verification-runtime.test.ts apps/cli/test/runtime/agent-projection-runtime.test.ts apps/cli/test/runtime/session-main-provider-continuation-runtime.test.ts packages/config/test/config.unit.test.ts packages/core-agent-projection/test/agent-projection-service.unit.test.ts`、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已通过，当前 round 收口为 `resolved`。
- [x] CR-006 sprint-001-acp-host-facing-transport-rollout delegated recheck loop round 6
  - 2026-04-15：任务创建，状态初始化为 `review_pending`。
  - 2026-04-15：fresh reviewer round 6 返回 1 条 actionable finding；主 agent 复核后判定为 `accepted`，指出 `acp_exec` surface 在 supervisor preflight 中先被 fail-closed 排除，导致 helper 层的 stale-slot cleanup 仍然无法覆盖真实 direct-answer 控制流。
  - 2026-04-15：accepted finding 已修复；当前 supervisor 会在 direct-answer preflight 结束后、进入 guard/fallback 之前，先尝试解析 selected surface 的 preflight continuation invalidation，并把该 mutation 合并到最终 outcome。新增 supervisor-level regression 覆盖 `remote_api -> acp_exec` guard path；同窗 `pnpm exec vitest run apps/cli/test/runtime/agent-onboarding-runtime.test.ts apps/cli/test/runtime/adapter-routing-runtime.test.ts apps/cli/test/runtime/adapter-verification-runtime.test.ts apps/cli/test/runtime/agent-projection-runtime.test.ts apps/cli/test/runtime/session-main-provider-continuation-runtime.test.ts apps/cli/test/runtime/session-main-supervisor-runtime.test.ts packages/config/test/config.unit.test.ts packages/core-agent-projection/test/agent-projection-service.unit.test.ts`、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已通过，当前 round 收口为 `resolved`。
- [x] CR-007 sprint-001-acp-host-facing-transport-rollout delegated recheck loop round 7
  - 2026-04-15：任务创建，状态初始化为 `review_pending`。
  - 2026-04-15：fresh reviewer round 7 返回 1 条 actionable finding；主 agent 复核后判定为 `accepted`，指出 role-delegate 的 single-role、serial、parallel preflight 仍未回收 preferred role surface 的 stale continuation slot。
  - 2026-04-15：accepted finding 已修复；当前 role-delegate preflight 会为 preferred role surface 统一尝试 continuation invalidation，并把 preflight mutations 合并到 single-role、serial、parallel outcome。新增 role-side regression 覆盖 single-role fallback、single-role guard、serial fallback、parallel fallback；同窗 `pnpm exec vitest run apps/cli/test/runtime/agent-onboarding-runtime.test.ts apps/cli/test/runtime/adapter-routing-runtime.test.ts apps/cli/test/runtime/adapter-verification-runtime.test.ts apps/cli/test/runtime/agent-projection-runtime.test.ts apps/cli/test/runtime/session-main-provider-continuation-runtime.test.ts apps/cli/test/runtime/session-main-supervisor-runtime.test.ts packages/config/test/config.unit.test.ts packages/core-agent-projection/test/agent-projection-service.unit.test.ts`、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` 已通过，当前 round 收口为 `resolved`。
- [x] CR-008 sprint-001-acp-host-facing-transport-rollout delegated recheck loop round 8
  - 2026-04-15：任务创建，状态初始化为 `review_pending`。
  - 2026-04-15：fresh reviewer round 8 返回 2 条 actionable finding；main agent 复核后均判定为 `accepted`，分别指向 role-delegate ACP unsupported-continuation regression coverage 缺口，以及 `acp_host_companion` diagnostics mapping regression coverage 缺口。当前任务推进到 `verified`，进入 accepted finding 修复阶段。
  - 2026-04-15：accepted findings 已全部修复；新增 ACP role-delegate unsupported-continuation regression coverage 与 ACP companion diagnostic-mapping regression coverage，并通过同窗 focused vitest、`pnpm run build` 与 `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1`。当前 round 收口为 `resolved`。
- [x] CR-009 sprint-001-acp-host-facing-transport-rollout delegated recheck loop round 9
  - 2026-04-15：任务创建，状态初始化为 `review_pending`。
  - 2026-04-15：fresh reviewer round 9 返回 clean 结论；main agent 复核后确认当前 sprint-001 boundary 内无剩余 actionable finding，`CR-009` 直接收口为 `resolved`。
