# checklist

- [x] TK-529 decompose cli borrowed capabilities draft into planned implementation rollout project and sprint packages
  - 2026-04-04：任务创建，状态初始化为 `planned`；范围限定为 docs-only decomposition，不引入新的 runtime 或 CLI 行为变更。
  - 2026-04-04：已新增 planned `project-043-cli-session-shell-productization-rollout`，并完成 `sprint-001 ~ sprint-003` 与 `TK-530 ~ TK-538` 的实体拆解。
