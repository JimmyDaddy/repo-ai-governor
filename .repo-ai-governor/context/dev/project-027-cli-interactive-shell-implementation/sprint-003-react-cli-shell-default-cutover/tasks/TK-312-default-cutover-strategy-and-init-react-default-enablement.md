# TK-312 默认切换策略与 `init` React 默认启用

- Status: planned
- Task ID: `TK-312`
- Sprint: `sprint-003-react-cli-shell-default-cutover`
- Project: `project-027-cli-interactive-shell-implementation`

## 1. 目标

把 `init` 的 React shell 切换为默认启用路径，同时保留 `classic` fallback 与显式禁用入口。

## 2. 产出

1. default switch policy
2. `init` default enablement
3. fallback gating
