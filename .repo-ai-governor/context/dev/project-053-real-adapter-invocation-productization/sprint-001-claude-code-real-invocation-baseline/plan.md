# sprint-001-claude-code-real-invocation-baseline 计划

- Status: completed
- Date: 2026-04-06
- Project: `project-053-real-adapter-invocation-productization`
- Sprint Goal: 建立 `Claude Code` 的第一条稳定真实调用产品路径。

## 1. Task Package

1. `TK-598` freeze Claude Code real invocation config probe timeout and degrade contract
2. `TK-599` implement Claude Code real invocation defaultable path with structured parsing and timeout handling
3. `TK-600` close Claude Code real invocation baseline with verify run and docs evidence

## 2. Exit Criteria

1. `Claude Code` 真实调用的 config/probe/timeout/degrade contract 已冻结。
2. 默认化真实调用路径与结构化 parsing 已落地。
3. `verify run` 与 docs evidence 已能支撑第一条真实调用基线。

## 3. Milestones

1. 2026-04-06：作为 `DA-588` follow-up decomposition 产物创建，当前保持 `planned`。
2. 2026-04-06：`project-052` final closeout 完成后被激活为当前 active sprint，`TK-598` 已切换为 `in_progress`。
3. 2026-04-07：`TK-598 ~ TK-600` 全部完成，`CR-001` clean resolved，`TK-607 / DA-607` 已完成 sprint closeout 并固定下一边界为 `sprint-002 / TK-601`。
