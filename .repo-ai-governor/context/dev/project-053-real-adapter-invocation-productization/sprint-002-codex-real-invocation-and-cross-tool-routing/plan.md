# sprint-002-codex-real-invocation-and-cross-tool-routing 计划

- Status: completed
- Date: 2026-04-06
- Project: `project-053-real-adapter-invocation-productization`
- Sprint Goal: 推进 `Codex` 真实调用，并验证跨工具 routing handoff。

## 1. Task Package

1. `TK-601` freeze Codex real invocation and cross-tool routing handoff contract
2. `TK-602` implement Codex real invocation fallback and route handoff hardening
3. `TK-603` close first-batch multi-tool real invocation routing acceptance

## 2. Exit Criteria

1. `Codex` 真实调用与 cross-tool routing handoff contract 已冻结。
2. fallback、route handoff 与 hardening 已落地。
3. 第一批 multi-tool 真实调用 acceptance 已可验证。

## 3. Milestones

1. 2026-04-06：作为 `project-053` 的第二个 planned sprint 创建，等待 `sprint-001` 稳定后激活。
2. 2026-04-07：在 `sprint-001` boundary commit `e75028f` 后被激活为当前 primary sprint，`TK-601` 已切换为 `in_progress`。
3. 2026-04-07：`TK-601 ~ TK-603` 已全部完成，`CR-001` resolved、`CR-002` clean resolved，`TK-608 / DA-608` 已完成 sprint closeout 并固定下一边界为 `sprint-003 / TK-604`。
