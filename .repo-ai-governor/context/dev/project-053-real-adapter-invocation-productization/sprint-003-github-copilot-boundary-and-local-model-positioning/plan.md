# sprint-003-github-copilot-boundary-and-local-model-positioning 计划

- Status: planned
- Date: 2026-04-06
- Project: `project-053-real-adapter-invocation-productization`
- Sprint Goal: 收口 `GitHub Copilot` 真实路径边界，并明确 `local-model` fallback 定位。

## 1. Task Package

1. `TK-604` freeze GitHub Copilot real invocation boundary and local-model fallback positioning
2. `TK-605` implement Copilot real path degrade handling and local-model support matrix alignment
3. `TK-606` close real adapter invocation rollout with support matrix and verify evidence refresh

## 2. Exit Criteria

1. `GitHub Copilot` 真实路径边界与 `local-model` fallback 定位已冻结。
2. degrade handling 与 support matrix alignment 已落地。
3. real adapter invocation rollout 已形成统一 docs / verify evidence refresh。

## 3. Milestones

1. 2026-04-06：作为 `project-053` 的第三个 planned sprint 创建，等待前两个 sprint 稳定后激活。
