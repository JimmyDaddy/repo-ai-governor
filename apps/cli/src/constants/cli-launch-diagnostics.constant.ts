/**
 * Declares the closed-set diagnostic codes projected into additive cli_exec launch diagnostics.
 */
export enum CliLaunchDiagnosticCode {
  SHELL_WRAPPED = 'protocol.shell_wrapped',
  PROCESS_TREE_POLICY = 'protocol.process_tree_policy',
  SPAWN_ERROR_CODE = 'install.spawn_error_code',
}
