# Shared Skill Assets

This directory stores shared skill assets for `Repo AI Governor`.

Examples:

1. shared templates
2. shared prompt fragments
3. shared helper scripts
4. shared reference files

These assets are not treated as standalone skills and should not contain their own `skill.json`.
