# Governor Context Loader Scripts

This skill is currently `instruction-only`.

Scripts are intentionally deferred until a later phase because context resolution is already deterministic from repository files.
