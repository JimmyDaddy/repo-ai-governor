# Release Ops Sprint 001 Checklist

- [x] **TK-901** 集成 `release-it + publish CI`（负责人：Release｜优先级：P0｜截止：2026-03-19｜状态：done）
  - 执行记录：plan=参考 `react-native-image-marker` 的 `release-it + CI` 思路，为当前仓库补齐发布配置、publish workflow、release gate 对齐和验证覆盖;result=已新增 `.release-it.json`、`.github/workflows/publish-npm.yml`、`release`/`release:dry-run` scripts、`publishConfig.provenance`、仓库元数据与 `package-lock.json`，并让 `release:check` 校验 `release-it` 配置和 publish workflow;verify=`PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/release-automation.test.js test/release/release-distribution.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run release:check && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm ci && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run check`
  - 执行记录：plan=收敛 `release-ga` 与 `publish-npm` 的职责边界，避免出现两个 workflow 都能执行 npm publish 的重复路径;result=已将 `.github/workflows/release-ga.yml` 改为 `Release GA (Manual)`，移除 `publish_npm` input 与旧的 `NPM_TOKEN` 发布步骤，明确 `publish-npm.yml` 为唯一 npm 发布入口，并同步更新 release 文档与自动化测试;verify=`PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/release-automation.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run check`
  - 执行记录：plan=修复 scoped package 名 `@cjhdev/repo-ai-governor` 带来的安装文档与 tarball 文件名影响，避免发布文档和 workflow 继续假设 unscoped 名称;result=已更新 `README.md`、`README.zh-CN.md`、`docs/quick-start.md`、`docs/getting-started-example.md` 的安装示例，明确“包名为 `@cjhdev/repo-ai-governor`、CLI 为 `repo-ai-governor`”，并让 `release-ga.yml` 动态解析 `npm pack --json` 生成的 tarball 文件名;verify=`PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/release-automation.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run release:check && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run check`
  - 执行记录：plan=修复 `run-getting-started-check.sh` 的 Homebrew 路径假设，让 `release:ga-check` 在 GitHub runner 上也能正确发现 `node` 和 `npm`;result=已将脚本改为“显式 env -> command -v -> /opt/homebrew/bin fallback”的解析顺序，并让 `test/release/getting-started-acceptance.test.js` 不再依赖本机 Homebrew PATH;verify=`PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/getting-started-acceptance.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run release:ga-check`
  - 执行记录：plan=排查 npm publish `E404` 并消除 CI 环境中残留 `NODE_AUTH_TOKEN` 对 trusted publishing 的干扰;result=已在 `.github/workflows/publish-npm.yml` 中将 `NODE_AUTH_TOKEN/NPM_TOKEN` 设为空，并在 publish 前执行 `npm config delete //registry.npmjs.org/:_authToken || true`，确保发布步骤走 OIDC trusted publisher；同时补充 `test/release/release-automation.test.js` 断言;verify=`PATH=/opt/homebrew/bin:$PATH npm run check && npm view @cjhdev/repo-ai-governor version --registry=https://registry.npmjs.org`
  - 执行记录：plan=继续排查 publish 阶段 `ENEEDAUTH`，对齐 npm Trusted Publishing 运行时要求并阻断 step 级 token 污染;result=已将 `publish-npm.yml` 升级为 `actions/setup-node@v6 + node-version 24`，并在 publish step 中显式 `env` 清空 + `unset NODE_AUTH_TOKEN/NPM_TOKEN`，最终执行 `npm publish --access public`；同步更新 `test/release/release-automation.test.js` 断言;verify=`PATH=/opt/homebrew/bin:$PATH node --test test/release/release-automation.test.js && PATH=/opt/homebrew/bin:$PATH npm run check`
  - 执行记录：review_delta=已完成 `TK-901` 自检复核，CR 结果落盘为 `code-review/verified_review_tk-901-integrate-release-it-publish-ci.md`;verify=复核确认当前推荐发布路径为 `npm run release` 触发 `release-it` 创建 GitHub Release，随后由 `publish-npm.yml` 在 Release published 事件中执行 npm publish

- [x] **TK-902** 修复 npm 安装后 `init` 首次上手可用性（负责人：Release｜优先级：P0｜截止：2026-03-19｜状态：done）
  - 执行记录：plan=修复 `npx @cjhdev/repo-ai-governor init` 在空目录下的首次体验，解决依赖未写入、`.repo-ai-governor` 目录空壳和 skills 未落地的问题;result=已在 `init` 中增加 npx 场景依赖自安装（写入 `package.json` + 执行安装）、默认启用 `codex` adapter、自动安装官方 skills、并在 `.repo-ai-governor/{adapters,slots,templates,reports}` 生成 README 模板避免空目录;verify=`PATH=/opt/homebrew/bin:$PATH node --test test/commands/init-command.test.js && PATH=/opt/homebrew/bin:$PATH npm run check`
  - 执行记录：review_delta=已完成 `TK-902` 自检复核，CR 结果落盘为 `code-review/verified_review_tk-902-fix-init-onboarding-from-npx.md`;verify=`npm_command=exec node ./bin/repo-ai-governor.js init --cwd <tmp> --project demo --sprint sprint-001 --format json` 输出包含 `dependencyBootstrap` 与 `skillBootstrap`
