# Repo AI Governor Support Matrix

- Status: active
- Last updated: 2026-04-09
- Scope: formal support declaration refreshed by `project-026 / sprint-004` (`TK-301`), `project-044 / sprint-003` (`TK-547`), `project-046 / sprint-001` (`TK-551`, `TK-552`, `TK-554`), `project-052 / sprint-001` (`TK-589`, `TK-590`, `TK-591`), `project-052 / sprint-002` (`TK-592`, `TK-593`, `TK-594`), `project-052 / sprint-003` (`TK-595`, `TK-596`), `project-053 / sprint-001` (`TK-598`, `TK-599`, `TK-600`), `project-053 / sprint-002` (`TK-601`, `TK-602`, `TK-603`), `project-053 / sprint-003` (`TK-604`, `TK-605`, `TK-606`), `project-054 / sprint-001` (`TK-607`, `TK-608`, `TK-609`), `project-054 / sprint-002` (`TK-610`), `project-055 / sprint-002` (`TK-616`), plus `project-061 / sprint-001 ~ sprint-006` (`TK-656` ~ `TK-667`) adoption-pack installer, self-host bootstrap, and clean-room truthfulness refresh

## 1. Installation Modes

| Mode | Status | Notes |
|---|---|---|
| `path` (`pnpm add <repo>`) | Supported | Default local adoption path for iterative development. |
| `link` (`pnpm add link:<repo>`) | Supported | Recommended only when the target repository should follow live governor source changes. |
| `dist-binary` (`node dist/bin/repo-ai-governor.js`) | Supported | Preferred no-install rehearsal path for dirty or non-`pnpm` repositories. |
| `tgz` (`pnpm pack` + `pnpm add <tarball>`) | Supported (online) | Requires registry/network reachability for runtime dependencies. |

### 1.1 Adopter Acceptance Contract

1. `Supported` means the documented baseline chain for that mode is reproducible under the preconditions named in the notes column and linked playbooks.
2. `path` is the default recommended install path for clean `pnpm` target repositories.
3. `link` stays supported, but only when the target repo is intentionally following local governor source changes.
4. `dist-binary` is the preferred path for dirty or non-`pnpm` target repositories and proves CLI/runtime behavior only; it does not prove packaged-install behavior.
5. `tgz` is limited to registry-enabled packaged-install rehearsal; offline or self-contained tarball installation remains unsupported.

## 2. Adapter Surfaces

| Adapter surface | Status | Notes |
|---|---|---|
| `codex` | Real-path available (environment-gated) | `cli_exec` is now the validated real transport for baseline `prepare -> execute -> report` dry-runs, and `verify --adapters` projects `codex` as the primary `cli_exec` route for `planner` / `architect` / `coder` / `reviewer` / `verifier` when the local Codex CLI is available. |
| `github-copilot` | Real-path available (environment-gated) | `cli_exec` is the default real transport when selected, `verify --adapters` now projects `github-copilot` tester routes with `transport=cli_exec`, and the local probe checks the direct `copilot` CLI first before falling back to `gh copilot -- --version`; quota/auth/probe failures still downgrade or reroute execution instead of being reported as governance-chain failure. |
| `claude-code` | Real-path available (environment-gated) | `cli_exec` is the default real transport when selected, `remote_api` remains optional, and `verify --adapters` now projects the effective default transport truth even when config omits `transport`; current workspace readiness still warns when the local Claude health-check fails. |
| `local-model` (`ollama`) | Fallback-only real-path (local-runtime constrained) | Endpoint-backed `ollama` probe/invoke truth is supported for restricted-network or explicit local fallback flows when endpoint/model config is present, but `tool_calling`, `structured_output`, and `confirmation_gate` remain conservative or unsupported by design; do not present this surface as a promoted primary substitute for the main remote adapters. |

### 2.1 Adapter Truth Labels

1. `Real-path available` means the adapter can expose non-fixture execution truth (`cli_exec` or optional `remote_api`) when selected, even if the current workspace still has environment-precondition warnings.
2. `Fallback-only real-path` means the adapter has real probe/invoke truth, but that truth is intentionally limited to fallback, restricted-network, or capability-constrained flows instead of a promoted primary adapter lane.
3. `Fixture-backed` means the product surface is supported, but the formal public evidence remains routing/fixture truth instead of a promoted real invocation path.

## 3. Published Governance Packs

| Pack | Status | Notes |
|---|---|---|
| TypeScript repository baseline | Built-in | Full governance chain in repository baseline. |
| Workflow review | Minimal baseline | `workflowReviewGovernancePack` is published via `@repo-ai-governor/standards` and carries the `CR-xxx` review task-card lifecycle. |
| Python | Minimal baseline | `pythonMinimalGovernancePack` is published via `@repo-ai-governor/standards`. |
| Go | Minimal baseline | `goMinimalGovernancePack` is published via `@repo-ai-governor/standards`. |

## 4. Runtime Baseline

| Item | Requirement / validated value |
|---|---|
| Node.js | Minimum `>=18` (engine contract); validated snapshot `v22.22.0` |
| pnpm | Package manager baseline; validated snapshot `10.30.3` |
| OS surface | macOS / Linux / WSL2 |

### 4.1 Public Command Surfaces

| Surface | Status | Notes |
|---|---|---|
| Bootstrap and audit (`init`, `doctor`, `check`) | Supported | Baseline external-adopter entry path for environment bootstrap and machine-readable governance facts. |
| Managed repo installation lifecycle (`adopt list/apply/diff/verify/upgrade/remove`) | Supported | Preferred whole-repository installer path for adopter baselines. Built-in packs materialize managed host assets, guides, and install metadata under `.repo-ai-governor/adoption/installations/**`; built-in `adopt apply` does not require a pre-existing source-local `.codex/skills/**` tree, while `self-host-complete` remains an explicit `repo_local` template-bootstrap path. |
| Adapter onboarding (`connect`, `verify`) | Supported | `connect` generates, diffs, and applies candidate configs; `verify --adapters` is the readiness gate before real execution. |
| Governed execution (`plan`, `run`, `review`, `review-verify`) | Supported | `plan` supports `preview|commit`; the review chain persists audit artifacts and can synchronize a canonical `CR-xxx` lifecycle when the workspace exposes sprint task surfaces. |
| Session shell (`repo-ai-governor`, `resume`) | Supported | Interactive TTY + `pretty` enters the session-first shell; `resume` can reattach the latest or named persisted session, while non-interactive surfaces stay on plain/json command semantics. |
| Workflow and schema lifecycle (`workflow`, `upgrade`) | Supported | `workflow` supports `preview/create/edit`; `upgrade` supports `preview/apply/rollback` with explicit hand-off artifacts and confirmation gates. |
| Workspace utilities and shell preferences (`workspace`, `set-ui-theme`) | Supported | Migration/rollback remain the main adopter contract; `clear-config`, `switch-branch`, and theme persistence are also formal public command surfaces. |
| Host distribution and service host (`host export/verify/pack`, `@cjhdev/repo-ai-governor/service-host`) | Supported for staged asset generation | This is the lower-level staged-asset path beneath the preferred `adopt apply` whole-repository install story. Use `host export --mode project-local` to stage host-native repo assets, optionally `--apply-to-repo` for repo-local hand-off, then `host verify` to validate the manifest/receipts; use `host pack --mode plugin-bundle` when the target supports installable bundles. Public host families currently cover `codex`, `claude-code`, and `github-copilot`; generation relies on repository-local `.codex/skills`, and the root-package `service-host` export is the only supported clean-room/desktop bootstrap import path. |

## 5. IDE / Execution Surface

| Surface | Packaging path | Status | Notes |
|---|---|---|---|
| CLI | published package / `dist-binary` | Supported | Primary production entry. |
| VS Code extension (`apps/vscode-extension`) | built source checkout + extension-development host | Supported for editor-native companion MVP | Build the governor repo first, then run the extension from `apps/vscode-extension`; trust-sensitive actions stay behind `Workspace Trust`, the extension must keep consuming service-owned query/command seams instead of creating extension-local execution shadow state, and the current MVP remains a companion surface rather than a replacement for CLI bootstrap or session-shell ownership. |
| VS Code packaged distribution (`npm` tarball / VSIX / Marketplace) | packaged installer | Not supported | The published governor tarball does not ship the `apps/vscode-extension` workspace or an installable extension bundle. It may still contain internal `dist/apps/vscode-extension/**` build artifacts, but those are not a supported npm/VSIX/Marketplace distribution. |
| Desktop sidecar entry | built source checkout / local distribution rehearsal | Supported for MVP foundation only | `apps/desktop` remains the foundation surface with service-owned session/execution/HITL/artifact-pane seams; `project-054` does not promote it into the preferred secondary surface or a packaged desktop product claim, and `check-desktop-entry-smoke` plus `release:verify-local` stay in the release baseline while richer desktop panels remain staged evolution. |

## 6. Verification Snapshot (TK-301 + TK-547 + TK-551/TK-552/TK-554 + TK-589/TK-590/TK-591 + TK-592/TK-593/TK-594 + TK-598/TK-599/TK-600 + TK-601/TK-602/TK-603 + TK-604/TK-605/TK-606 + TK-607/TK-608/TK-609 + TK-610 + TK-614/TK-615 + TK-616 + TK-656~TK-667)

| Time (UTC) | Command | Result | Evidence |
|---|---|---|---|
| 2026-04-08T19:37:55Z | `project-061 clean-room adoption rehearsal (adopt apply/verify/diff/upgrade/remove + self-host apply/verify)` | Pass | `.tmp/project-061-adoption-pack-cleanroom-summary.json` now records a clean multi-host `adopter-complete` install/verify, a clean lifecycle `diff -> upgrade -> remove` slice, and a `self-host-complete + repo_local` bootstrap/verify slice. The evidence also confirms the managed metadata layout under `.repo-ai-governor/adoption/installations/**` and the absence of any pre-existing `.codex/skills/**` requirement for built-in `adopt apply`. |
| 2026-04-08T18:54:12Z | `pnpm exec vitest run apps/cli/test/adopt-command.integration.test.ts apps/cli/test/cli-skeleton.integration.test.ts packages/standards/test/adoption-pack-registry.unit.test.ts --maxWorkers=1 --maxConcurrency=1` | Pass | The adoption-pack verification slice kept CLI help/catalog, layered registry resolution, multi-host `adopt apply`, clean `adopt upgrade`, `adopt diff/remove`, and `self-host-complete` template bootstrap green (`3` files / `11` tests). |
| 2026-04-08T14:54:37Z | `pnpm exec vitest run apps/cli/test/cli-skeleton.integration.test.ts apps/cli/test/cli-output-contract.integration.test.ts apps/cli/test/commands/workflow-command.test.ts apps/cli/test/commands/workspace-command.test.ts apps/cli/test/commands/host-command.test.ts apps/cli/test/host-command.integration.test.ts --maxWorkers=1 --maxConcurrency=1` | Pass | Command-surface verification slice kept the public help/catalog, session-shell entry/resume contract, workflow preview/create/edit, workspace clear-config plus theme persistence, and host export/verify/pack behavior green (`6` files / `89` tests). |
| 2026-04-07T04:23:35Z | `dist-binary pilot-2 rehearsal (init -> doctor -> check -> upgrade preview/apply/rollback -> workspace dry-run/execute/rollback)` | Pass | `.tmp/project-055-sprint-001-pilot-2-rehearsal-summary.json` captured the recovered `react-native-image-marker-1.1.x` acceptance rerun in `5326ms`; the onboarding subchain (`init -> doctor -> check`) completed in `1711ms`, `workspace execute` switched to `repo_local`, rollback returned to `tool_managed`, `gitStatusPreserved=true`, and scratch cleanup finished with `removed`. |
| 2026-04-07T04:09:36Z | `link-install pilot-1 rehearsal (pnpm install -> init -> doctor -> check -> verify --adapters -> run --dry-run --trace)` | Pass | `.tmp/project-055-sprint-001-pilot-1-rehearsal-summary.json` recorded a full `playground` adopter-path rehearsal in `50473ms`; `required_role_failures=0`, the only adapter degrade/fallback stayed non-blocking, and the traced dry-run completed successfully with replay/report/diagnostics artifacts. |
| 2026-04-07T03:30:14Z | `pnpm exec vitest run apps/vscode-extension/test/vscode-extension-service-runtime.test.ts apps/vscode-extension/test/vscode-extension-contract.test.ts apps/vscode-extension/test/vscode-extension-controller-and-provider.test.ts apps/vscode-extension/test/vscode-extension-presentation-builder.test.ts apps/vscode-extension/test/vscode-extension-selection-store.test.ts apps/vscode-extension/test/vscode-extension-packaging-boundary.test.ts --maxWorkers=1 --maxConcurrency=1` | Pass | The VS Code extension service-runtime degradation guard, contract, controller/provider flows, presentation mapping, selection-store lifecycle, and packaging-boundary guard all passed in one targeted verification slice after the reviewer finding fix (`6` files / `12` tests). |
| 2026-04-07T02:19:06Z | `pnpm run build` | Pass | Build refreshed `dist/apps/vscode-extension` and preserved the extension host entrypoints consumed by `apps/vscode-extension/package.json`. |
| 2026-04-07T02:19:31Z | `pnpm pack --json --dry-run` | Pass | The dry-run package manifest confirmed that the published tarball omits the `apps/vscode-extension` workspace, manifest files, and resources while still carrying internal `dist/apps/vscode-extension/**` build artifacts only as non-installable runtime payloads. |
| 2026-04-07T02:20:00Z | `pnpm run check:ide-entry-smoke` | Pass | IDE entry smoke kept the VS Code / JetBrains / Cursor / Claude Code example entry assets coherent while the public support boundary continued to keep the real VS Code extension on the source-checkout path. |
| 2026-04-07T02:20:23Z | `pnpm run check:ide-docs-parity` | Pass | IDE template docs parity stayed green across the checked `integrations/ide/**` surfaces; packaged-artifact truth for the public VS Code boundary remains evidenced by the packaging-boundary test plus `pnpm pack --json --dry-run`, while the support-matrix/playbook/README updates were made in the same closeout window. |
| 2026-04-06T23:37:45Z | `node ./dist/bin/repo-ai-governor.js --output json --adapters --dry-run --trace run` | Pass | `.tmp/project-053-sprint-003-run-dry-run-trace-tk-605-606.json` plus the emitted report/replay/diagnostics artifacts preserve `dry_run=true`, `policy_outcome=allow`, and a fully successful baseline `prepare -> execute -> report` run while keeping project-final replay and trace evidence ready for review. |
| 2026-04-06T23:37:26Z | `node ./dist/bin/repo-ai-governor.js --output json --adapters verify` | Warn (non-blocking) | `.tmp/project-053-sprint-003-verify-adapters-tk-605-606.json` and `/Users/jimmydaddy/.repo-ai-governor/workspaces/2cf23e5951f0/.repo-ai-governor/context/diagnostics/verify/verify-1775518628055.json` now project `role_tester` as `selected=github-copilot transport=cli_exec`; remaining warns stay limited to tool-managed workspace bootstrap truth instead of required-role failure. |
| 2026-04-06T23:36:54Z | `pnpm vitest run packages/adapters/github-copilot/test/github-copilot-agent-adapter.smoke.test.ts packages/adapters/local-model/test/local-model-agent-adapter.smoke.test.ts apps/cli/test/runtime/adapter-verification-runtime.test.ts test/first-batch-adapters-route.integration.test.ts --maxWorkers=1 --maxConcurrency=1` | Pass | Sprint-003 targeted verification passed across `github-copilot`, `local-model`, adapter-verification runtime projection, and first-batch routing acceptance in one slice (`4` files, `42` tests). |
| 2026-04-06T17:45:29Z | `node ./dist/bin/repo-ai-governor.js --output json --adapters --dry-run --trace run` | Pass | `.tmp/project-053-sprint-002-run-dry-run-trace.json` plus the emitted report/replay/diagnostics artifacts now preserve `dry_run=true`, `policy_outcome=allow`, and a fully successful baseline `prepare -> execute -> report` run on the default `codex` primary route. |
| 2026-04-06T17:44:58Z | `node ./dist/bin/repo-ai-governor.js --output json --adapters verify` | Warn (non-blocking) | `.tmp/project-053-sprint-002-verify-adapters.json` and the verify diagnostics artifact project `codex` as primary `cli_exec` for `planner` / `architect` / `coder` / `reviewer` / `verifier`; remaining warns are limited to durable-storage, artifact-registry, and task-ledger initialization truth in the tool-managed workspace. |
| 2026-04-06T16:26:31Z | `node ./dist/bin/repo-ai-governor.js --output json --adapters --dry-run --trace run` | Warn | `.tmp/project-053-sprint-001-run-dry-run-trace.json` plus the emitted report/replay/diagnostics artifacts preserved `dry_run=true`, `policy_outcome=allow`, and stage-level failure attribution at `stage-task-prepare`, even though the current default route still failed on `codex`. |
| 2026-04-06T16:25:22Z | `node ./dist/bin/repo-ai-governor.js --output json --adapters verify` | Warn (non-blocking) | `.tmp/project-053-sprint-001-verify-adapters.json` and the verify diagnostics artifact surfaced `claude-code` as effective `cli_exec` with `request_timeout_ms=30000`, `max_retries=2`, and an environment-precondition health-check warning instead of a silent `null` transport. |
| 2026-04-06T21:29:47Z | `repo-external upgrade rehearsal (preview -> apply -> rollback)` | Pass | `.tmp/project-052-sprint-002-command-rehearsal-summary.json` recorded `schema_upgrade_analyze`, `schema_upgrade_apply`, and `schema_upgrade_rollback`; apply and rollback both finished with `verify_status=passed`. |
| 2026-04-06T21:29:47Z | `repo-external workspace rehearsal (dry-run -> execute -> rollback)` | Pass | `.tmp/project-052-sprint-002-command-rehearsal-summary.json` recorded `workspace_migration_plan`, `workspace_migration_execute`, and `workspace_migration_rollback`; rollback returned to the source workspace and `scratch_cleanup_status=removed`. |
| 2026-04-06T12:09:11Z | `node ./scripts/release/verify-cleanroom-local-install.js --modes path,link --iterations 1 --output .tmp/project-052-sprint-001-cleanroom-report.json` | Pass | `path` and `link` both passed one clean-room baseline iteration; workspace switch rollback, read-only attach precheck, service-host memory provider, and remote-api smoke also passed for both modes. |
| 2026-04-06T12:08:49Z | `node ./scripts/release/verify-local-distribution.js --output .tmp/project-052-sprint-001-local-distribution-report.json` | Pass | local distribution verified, `pack_file=cjhdev-repo-ai-governor-0.1.5.tgz`; standards runtime-loader dist smoke and dist-binary remote-api smoke passed, while adapter `doctor/verify` stayed at non-blocking `warn`. |
| 2026-04-04T12:09:14Z | `pnpm run build` | Pass | `dist/apps/desktop` and `dist/node_modules/@repo-ai-governor/desktop` materialized for local distribution validation |
| 2026-04-04T12:09:14Z | `pnpm run test:packages -- --maxWorkers=1 --maxConcurrency=1` | Pass | `118` files and `734` tests passed |
| 2026-04-04T12:09:14Z | `pnpm run test:integration -- --maxWorkers=1 --maxConcurrency=1` | Pass | `19` files and `47` tests passed |
| 2026-04-04T12:12:23Z | `pnpm run release:verify-local` | Pass | CLI help smoke + desktop entry smoke + examples runtime smoke + dist-binary remote-api smoke + packed-surface truthfulness all passed |
| 2026-04-05T02:13:09Z | `pnpm vitest run apps/desktop/test/desktop-governance-console-view-model-builder.test.ts apps/desktop/test/desktop-shell-bootstrap.test.ts apps/desktop/test/desktop-session-bridge.test.ts test/desktop-entry-smoke.integration.test.ts --maxWorkers=1 --maxConcurrency=1` | Pass | Desktop artifact-pane query contract, preload bridge, shell baseline, and smoke integration all passed against the ready-state gate. |
| 2026-04-05T02:17:45Z | `pnpm vitest run packages/adapters/github-copilot/test/github-copilot-agent-adapter.smoke.test.ts packages/adapters/claude-code/test/claude-code-agent-adapter.smoke.test.ts packages/adapters/local-model/test/local-model-agent-adapter.smoke.test.ts test/first-batch-adapters-route.integration.test.ts --maxWorkers=1 --maxConcurrency=1` | Pass | `github-copilot`, `claude-code`, `local-model`, and first-batch routing fallback coverage all passed in one targeted verification slice. |
| 2026-03-27T22:38:31Z | `node ./dist/bin/repo-ai-governor.js doctor --output pretty` | Pass | `attach_mode=read_write`, `operation=env_doctor` |
| 2026-03-27T22:38:38Z | `node ./dist/bin/repo-ai-governor.js verify --output pretty --adapters` | Warn (non-blocking) | `adapters_status=warn`, required role failures `0` |
| 2026-03-27T22:39:17Z | `node ./dist/bin/repo-ai-governor.js workspace --workspace-action dry-run --workspace-mode repo_local --output pretty` | Pass | workspace plan emitted under active workspace root |
| 2026-03-27T22:39:21Z | `node ./scripts/release/verify-local-distribution.js` | Pass | local distribution verified, `pack_file=cjhdev-repo-ai-governor-0.1.5.tgz` |
| 2026-03-27T22:39:31Z | `node ./scripts/examples/check-desktop-entry-smoke.js` | Pass | desktop sidecar runtime smoke passed for default distribution mode |

## 7. Upgrade / Workspace Contract Snapshot (TK-592)

1. `workspace` formal adopter path is `dry-run -> execute -> rollback`; `rollback` only consumes the saved `plan-path`, and failed execute flows persist `context/workspace/<migration-id>.failure.json` before retry.
2. `upgrade` formal adopter path is `preview -> apply -> rollback`; `apply` only consumes one preview `report_path` plus explicit `--confirm-upgrade approve`, and rollback accepts one apply receipt or rollback snapshot.
3. `docs/local-adoption-playbook*.md` is the canonical adopter guide for artifact hand-off and troubleshooting, while `README*` keeps only the minimal command surface.

## 8. Troubleshooting / Acceptance Snapshot (TK-594)

1. Save `plan_path`, `report_path`, and at least one rollback hand-off artifact (`apply_receipt_path` or `rollback_snapshot_path`) before mutating adopter state; the supported closeout path depends on those artifacts.
2. If `upgrade` preview reports blocking confirmation items, stop before `apply`, review the preview artifacts, and rerun preview only after the configuration drift is understood.
3. After `workspace execute` or `workspace rollback`, rerun `doctor` to confirm the active `workspaceRoot` instead of inferring success from directory layout alone.
4. Rehearsal and pilot runs should use target repositories or isolated external temp directories; running workspace migration from the governor source repository can attach to the outer Git root and create misleading artifacts.
5. `.tmp/project-052-sprint-002-command-rehearsal-summary.json` is the formal sprint-002 acceptance evidence for the repo-external upgrade/workspace closeout path.

## 9. GA Support Truthfulness Snapshot (TK-596)

1. `docs/support-matrix*.md` is now the single public truth surface for current support status plus GA support truthfulness.
2. `docs/maintainer-validation-playbook*.md` remains the maintainer runbook and backlink router; it should not keep a parallel support-status table.
3. `docs/ga-readiness-evidence*.md` remains the program-level signal matrix and may backlink to this section, but it does not redefine the public support boundary.

| Claim scope | Audience | Surface | Status | Evidence time (UTC) | Evidence command / artifact | Evidence summary | Backlink target | Refresh trigger | Residual risk |
|---|---|---|---|---|---|---|---|---|---|
| adoption-pack installer + self-host bootstrap | adopter + maintainer | managed `adopt` repo-installation story | Pass | 2026-04-08T19:37:55Z | `.tmp/project-061-adoption-pack-cleanroom-summary.json` | Built-in `adopt apply` now installs a managed multi-host adopter baseline and `self-host-complete` repo-local template bootstrap without requiring a pre-existing source-local `.codex/skills/**` tree; the same evidence packet also confirms clean `diff/upgrade/remove` lifecycle behavior and the current installer metadata layout under `.repo-ai-governor/adoption/installations/**`. | `README.md`, `docs/local-adoption-playbook.md` | `adopt` contract, managed metadata layout, or self-host template semantics changes | Multi-host installs currently keep the receipt/backlink chain centered on the primary host apply report even though managed-file verification still covers the full projected file set. |
| VS Code secondary surface boundary | adopter + maintainer | source-checkout editor-native companion | Pass | 2026-04-07 | `apps/vscode-extension/README.md`, `docs/support-matrix.md`, `docs/local-adoption-playbook.md`, `docs/maintainer-validation-playbook.md`, `pnpm exec vitest run apps/vscode-extension/test/vscode-extension-service-runtime.test.ts apps/vscode-extension/test/vscode-extension-contract.test.ts apps/vscode-extension/test/vscode-extension-controller-and-provider.test.ts apps/vscode-extension/test/vscode-extension-presentation-builder.test.ts apps/vscode-extension/test/vscode-extension-selection-store.test.ts apps/vscode-extension/test/vscode-extension-packaging-boundary.test.ts --maxWorkers=1 --maxConcurrency=1`, `pnpm pack --json --dry-run`, `pnpm run check:ide-entry-smoke`, `pnpm run check:ide-docs-parity` | The support boundary now explicitly promotes the real VS Code extension as the preferred secondary surface only for built source-checkout usage. Packaged npm/VSIX distribution remains outside formal support because the packaging-boundary test, the service-runtime degrade-path guard, and the dry-run package manifest show only non-installable internal `dist/apps/vscode-extension/**` runtime payloads, while `check:ide-docs-parity` covers the checked IDE template-doc surfaces rather than this public support-truth table itself. | `docs/local-adoption-playbook.md`, `docs/maintainer-validation-playbook.md`, `apps/vscode-extension/README.md` | extension packaging path, trust-gating contract, or secondary-surface support narrative changes | A future official VSIX or packaged delivery path will require a new packaging truth row plus dedicated rehearsal evidence. |
| clean-room install baseline | adopter + maintainer | `path` / `link` install modes | Pass | 2026-04-06T12:09:11Z | `.tmp/project-052-sprint-001-cleanroom-report.json` | `path` and `link` both passed the clean-room chain and covered workspace-switch rollback prechecks. | `docs/maintainer-validation-playbook.md`, `.tmp/project-052-sprint-001-cleanroom-report.json` | install-mode contract or packaged runtime changes | Wider `tgz` / registry-backed packaged install still depends on separate packaged distribution rehearsal. |
| packaged distribution rehearsal | maintainer | local distribution / packaged surface | Pass | 2026-04-06T12:08:49Z | `.tmp/project-052-sprint-001-local-distribution-report.json` | Local distribution verified the packed governor surface and kept adapter `doctor/verify` as non-blocking warn rather than a hard support failure. | `docs/maintainer-validation-playbook.md`, `.tmp/project-052-sprint-001-local-distribution-report.json` | packaging layout, release asset, or dist runtime changes | Adapter warn semantics remain environment-precondition dependent. |
| repo-external upgrade/workspace closeout | adopter + maintainer | `upgrade` and `workspace` user path | Pass | 2026-04-06T21:29:47Z | `.tmp/project-052-sprint-002-command-rehearsal-summary.json` | External rehearsal passed `preview -> apply -> rollback` and `dry-run -> execute -> rollback`, including rollback verification and scratch cleanup. | `docs/local-adoption-playbook.md`, `.tmp/project-052-sprint-002-command-rehearsal-summary.json` | command contract, rollback artifact semantics, or troubleshooting flow changes | The contract is now also exercised by the real-target `project-055` pilot dossier; future rollback or artifact hand-off changes should refresh both evidence packets together. |
| real adapter invocation rollout | adopter + maintainer | multi-adapter support truth (`claude-code`, `codex`, `github-copilot`, `local-model`) | Pass | 2026-04-06T23:37:45Z | `.tmp/project-053-sprint-003-verify-adapters-tk-605-606.json`, `.tmp/project-053-sprint-003-run-dry-run-trace-tk-605-606.json` | The support matrix now distinguishes `claude-code` / `codex` / `github-copilot` real-path availability from `local-model` fallback-only real-path, and the fresh dry-run trace preserved the replay/report/diagnostics chain for the default CLI-backed baseline. | `docs/local-adoption-playbook.md`, `.tmp/project-053-sprint-003-run-dry-run-trace-tk-605-606.json` | adapter routing contract, support-label semantics, or verify projection changes | `verify --adapters` still reports non-blocking tool-managed workspace bootstrap warnings unrelated to required-role failures. |
| real adopter pilot dossier | adopter + maintainer + project-closeout | `playground` + `react-native-image-marker-1.1.x` real-target pilot packet | Pass | 2026-04-07T04:23:35Z | `.tmp/project-055-sprint-001-pilot-1-rehearsal-summary.json`, `.tmp/project-055-sprint-001-pilot-2-rehearsal-summary.json`, `.repo-ai-governor/context/dev/project-055-ga-evidence-and-adopter-pilot-closeout/sprint-002-ga-evidence-consolidation-and-closeout/tasks/DA-616-ga-evidence-dossier-and-cross-surface-backlinks.md` | Two real target repositories now back the supported `link` onboarding path and the `dist-binary` plus `upgrade/workspace` closeout path, while the dossier explicitly preserves the recovered-baseline caveat on the complex pilot instead of overstating continuity. | `docs/ga-readiness-evidence.md`, `docs/maintainer-validation-playbook.md`, `.repo-ai-governor/context/dev/project-055-ga-evidence-and-adopter-pilot-closeout/sprint-002-ga-evidence-consolidation-and-closeout/tasks/DA-616-ga-evidence-dossier-and-cross-surface-backlinks.md` | pilot repo choice, onboarding chain, or workspace rollback semantics change | The complex pilot claim is intentionally limited to the recovered `1.1.x` rerun because the original frozen working copy was interrupted by an operator misconfiguration. |
| maintainer release runbook | maintainer | local release-gate rehearsal | Pass | 2026-04-04T12:12:23Z | `pnpm run release:verify-local` | The maintainer gate still proves CLI help smoke, desktop entry smoke, examples runtime smoke, dist-binary remote-api smoke, and packed-surface truthfulness in one runbook step. | `docs/maintainer-validation-playbook.md` | release gate composition or packaged surface changes | This row is a runbook-backed rehearsal, not a new public support contract. |
| program-level GA signals | maintainer + project-closeout | cross-stage GA readiness | Pass | 2026-04-07 | `docs/ga-readiness-evidence.md` | The broader GA signal matrix remains green and is now refreshed with the real-target pilot evidence from `project-055`, while still back-linking to this support truth surface instead of serving as a parallel public claim. | `docs/ga-readiness-evidence.md` | GA signal thresholds or upstream evidence refresh | The prepared `project-055` closeout recommendation now depends only on the clean sprint/project review loops and the final closeout write-back. |

4. The prepared `project-055` closeout recommendation now lives in `.repo-ai-governor/context/dev/project-055-ga-evidence-and-adopter-pilot-closeout/project-055-ga-evidence-and-adopter-pilot-closeout-completion-audit-summary.md`; the final `completed` verdict now depends only on the clean sprint/project review loops and the final completion write-back.

## 10. Notes

1. Adapter degrade or warning signals remain environment-precondition related (`github-copilot` quota/probe, `claude-code` CLI health-check/auth preconditions, `local-model` endpoint/model capability limits) rather than governance-chain failure.
2. `project-046` promotes the desktop artifact pane from deferred gate to a service-owned typed query contract; renderer consumers still do not bypass the workspace filesystem directly.
3. Official `GitLab CI` and `Jenkins` templates are now published under `integrations/ci/` and reuse the same install, quality-gate, and release-governance command contract as GitHub Actions.
4. This matrix defines current formal support boundaries for `TK-301`, the desktop MVP foundation refresh in `TK-547`, the P1 product-surface closure work in `project-046`, the install-mode truth refresh in `project-052 / sprint-001`, the upgrade/workspace contract plus acceptance closeout in `project-052 / sprint-002`, the GA support truthfulness consolidation in `project-052 / sprint-003`, the `claude-code` real-path baseline truth refresh in `project-053 / sprint-001`, the `codex` real-path plus routed dry-run acceptance refresh in `project-053 / sprint-002`, the `github-copilot` real-path plus `local-model` fallback-only positioning closeout in `project-053 / sprint-003`, the VS Code secondary-surface packaging/support freeze in `project-054 / sprint-001`, the frozen VS Code MVP gap list plus desktop foundation non-goal guardrails in `project-054 / sprint-002 / TK-610`, the real-target pilot dossier plus GA evidence refresh in `project-055 / sprint-002 / TK-616`, the `project-061` adoption-pack installer/self-host/bootstrap truth sync for `TK-656 ~ TK-667`, and the 2026-04-09 public command-surface truth sync for `adopt` plus the surrounding session/workflow/workspace/host/service-host entrypoints; broader GA readiness signal closure now continues through `project-057` and `project-056`.
