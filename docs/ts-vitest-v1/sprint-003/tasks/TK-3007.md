# TK-3007 收敛 literal-set whitelist 存量并分批迁移

- Status: in-progress
- Priority: P0
- Project: `ts-vitest-v1`
- Sprint: `sprint-003`
- Owner: Release
- Due: 2026-04-13
- Depends On: `TK-3001`

## Goal

将 `scripts/governance/literal-set-whitelist.json` 当前承接的 13 个文件转化为可执行迁移批次，逐批迁出白名单并收敛到 `src/constants`。

## Deliverables

1. 完成 literal-set 白名单条目到迁移批次的全量映射
2. 为每个批次定义实施顺序、完成标准与验证命令
3. 每完成一批，删除对应 whitelist 条目并记录执行结果

## Batch Plan

1. `LS-B1` CLI 与配置基础集（4 文件）
   - `src/cli/runtime/exit-codes.ts`
   - `src/cli/ui/theme.ts`
   - `src/config/repository-layout.ts`
   - `src/config/schema/index.ts`
2. `LS-B2` Skills 与 Slots 运行时集（4 文件）
   - `src/skills/package-layout.ts`
   - `src/skills/runtime.ts`
   - `src/slots/runtime.ts`
   - `src/slots/slot-model.ts`
3. `LS-B3` 工作流与标准模型集（3 文件）
   - `src/standards/package-model.ts`
   - `src/workflow/governance-engine.ts`
   - `src/workflow/template-model.ts`
4. `LS-B4` 业务入口与适配器尾项（2 文件）
   - `src/commands/run-command.ts`
   - `src/adapters/adapter-model.ts`

## Acceptance

1. 13 个白名单路径全部映射到明确批次
2. 每批完成后 `check-finite-literal-sets` 通过且无新增临时豁免
3. `literal-set-whitelist.json` 条目数按批次持续下降并可审计

## Verification

1. `node ./scripts/governance/check-finite-literal-sets.js --format=json`
2. `npm run typecheck`
3. `npm run test -- test/governance/finite-literal-set-gate.test.ts`
