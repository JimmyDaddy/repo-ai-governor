# TK-2005 迁移剩余测试到 TypeScript

- Status: done
- Priority: P1
- Project: `ts-vitest-v1`
- Sprint: `sprint-002`
- Owner: QA
- Due: 2026-04-02
- Depends On: `TK-2002`, `TK-2003`, `TK-2004`

## Goal

将剩余 `.test.js` 分批迁移到 `.test.ts`，统一测试层类型体验并减少 JS 残留。

## Deliverables

1. 核心回归测试文件迁移为 `.test.ts`
2. 保持 Vitest 配置与覆盖统计稳定
3. 补充迁移批次记录

## Acceptance

1. 关键路径测试已迁移为 TS
2. `npm test` 与 `npm run test:coverage` 通过
3. 测试行为无回归

## Progress

1. 已完成第一批命令测试迁移：`test/commands/review-command.test.ts`、`test/commands/review-verify-command.test.ts`，并删除对应 `.test.js`。
2. 已补齐测试层最小类型边界：缓冲输出流、命令执行返回结构、`execFileSync` 选项与辅助函数参数显式化。
3. 首批验证通过：`npm run typecheck`、`npm run test -- test/commands/review-command.test.ts test/commands/review-verify-command.test.ts`、`npm run check`。
4. 全量回归补充通过：`npm run test` 与 `npm run release:ga-check`。
5. 已完成第二批命令测试迁移：`test/commands/{automation-shared,check-command,doctor-command,init-command,plan-command,report-command,run-command,skills-command,upgrade-command}.test.ts`，并删除对应 `.test.js`。
6. 已收敛第二批迁移中的类型问题：批量补齐测试辅助函数参数类型、缓冲流数组类型与路由索引映射返回类型，保持断言行为不变。
7. 二批验证通过：`npm run typecheck`、`npm run test -- test/commands/*.test.ts`、`npm run check`、`npm run test`、`npm run test:coverage`、`npm run release:ga-check`。
8. 当前剩余 `.test.js` 数量已降至 26（主要位于 `acceptance/adapters/ci/config/release/skills/slots/standards/workflow` 分组），下一批将继续迁移非命令测试。
9. 已完成第三批非命令测试迁移：`test/{acceptance,adapters,ci,config,governance,release,skills,slots,standards,workflow}/*.test.ts` 全量切换并删除对应 `.test.js`，当前 `test/**` 下无 `.test.js` 残留。
10. 已收敛第三批迁移类型问题：补齐 smoke payload 类型、缓冲流与命令参数显式类型、slot 配置 helper 断言，并修复发布脚本并发打包冲突导致的 tarball 安装不稳定。
11. 三批最终验证通过：`npm run typecheck`、`npm run test -- test/acceptance/*.test.ts test/adapters/*.test.ts test/ci/*.test.ts test/config/*.test.ts test/governance/*.test.ts test/release/*.test.ts test/skills/*.test.ts test/slots/*.test.ts test/standards/*.test.ts test/workflow/*.test.ts`、`npm run check`、`npm run test`、`npm run test:coverage`、`npm run release:ga-check`。
