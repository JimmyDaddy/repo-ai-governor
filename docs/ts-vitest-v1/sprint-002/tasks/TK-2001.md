# TK-2001 清理试点模块临时类型豁免并收敛强类型

- Status: done
- Priority: P0
- Project: `ts-vitest-v1`
- Sprint: `sprint-002`
- Owner: Core
- Due: 2026-03-29
- Depends On: `TK-1003`, `TK-1007`

## Goal

移除试点模块中的临时 `@ts-nocheck` 与弱类型占位，确保试点目录在 `strict` 下具备稳定类型约束。

## Deliverables

1. 清理 `validator/reporting` 试点文件中的 `@ts-nocheck`
2. 补充必要类型定义（输入/输出/错误结构）
3. 回归验证与风险说明

## Acceptance

1. 试点模块无 `@ts-nocheck`
2. `npm run typecheck` 通过
3. 试点相关测试通过且无行为回归

## Progress

1. 已完成第一轮清理：`src/config/schema/validator.ts`、`src/reporting/report-model.ts`、`src/reporting/report-source.ts` 的 `@ts-nocheck` 已移除并补齐类型定义。
2. 验证已通过：`npm run typecheck`、`npm run test -- test/config/schema.test.ts test/reporting/report-model.test.ts test/commands/report-command.test.js`、`npm run check`。
3. `test/config/schema.test.ts` 仍保留 `@ts-nocheck`，计划在 `TK-2005` 的测试迁移批次统一收敛。
4. 已完成自检复核，CR 记录为 `code-review/verified_review_tk-2001-cleanup-pilot-ts-nocheck.md`。
