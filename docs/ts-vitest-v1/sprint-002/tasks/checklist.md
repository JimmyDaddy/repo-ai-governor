# TS Vitest V1 Sprint 002 Checklist

- [x] **TK-2001** 清理试点模块临时类型豁免并收敛强类型（负责人：Core｜优先级：P0｜截止：2026-03-29｜状态：done）
  - 执行记录：plan=移除试点模块中的临时 `@ts-nocheck` 与弱类型占位，补充必要类型定义并保证行为不变;result=待执行;verify=待执行
  - 执行记录：plan=先收敛试点源码模块类型，移除 `validator/reporting` 的 `@ts-nocheck` 并补充显式类型定义;result=已完成 `src/config/schema/validator.ts`、`src/reporting/{report-model,report-source}.ts` 的类型化改造并移除 `@ts-nocheck`，当前 `typecheck` 与相关回归通过;verify=`npm run typecheck && npm run test -- test/config/schema.test.ts test/reporting/report-model.test.ts test/commands/report-command.test.js && npm run check`
  - 执行记录：review_delta=已完成 `TK-2001` 自检复核，CR 结果落盘为 `code-review/verified_review_tk-2001-cleanup-pilot-ts-nocheck.md`;verify=`rg -n "@ts-nocheck" src/config/schema src/reporting || true`

- [x] **TK-2002** 迁移 `workflow/slots/standards/config` 到 TypeScript（负责人：Core｜优先级：P0｜截止：2026-03-30｜状态：done）
  - 执行记录：plan=将核心引擎与配置读取链路迁移为 TS，保留 ESM 导入兼容并通过构建与回归;result=待执行;verify=待执行
  - 执行记录：plan=分批迁移 workflow/slots/standards/config 到 `.ts` 并清理同路径 `.js`；优先保证 typecheck 与核心链路测试稳定;result=已完成 `src/workflow/*`、`src/slots/*`、`src/standards/*`、`src/config/{errors,repository-layout,load-config}` 的 TypeScript 迁移并删除旧 `.js`，核心域类型与执行模型已补齐;verify=`npm run typecheck && npm run test -- test/workflow/template-model.test.js test/workflow/governance-engine.test.js test/slots/slot-model.test.js test/slots/runtime.test.js test/standards/package-model.test.js test/standards/official-base-package.test.js test/config/repository-layout.test.js test/config/load-config.test.js && npm run check`
  - 执行记录：review_delta=已完成 `TK-2002` 自检复核，CR 结果落盘为 `code-review/verified_review_tk-2002-migrate-core-workflow-slots-standards-config-to-ts.md`;verify=`rg --files src/workflow src/slots src/standards src/config | sort`

- [x] **TK-2003** 迁移 `adapters/skills` 与 examples 脚本到 TypeScript（负责人：Core｜优先级：P1｜截止：2026-03-31｜状态：done）
  - 执行记录：plan=迁移 adapter/skills 模块及示例渲染脚本，确保多适配器输出一致;result=待执行;verify=待执行
  - 执行记录：plan=完成 `src/adapters/*` 与 `src/skills/*` 向 `.ts` 迁移并删除同路径 `.js`，同时保持 examples 渲染脚本输出行为稳定;result=已完成 `src/adapters/{adapter-model,bundle-shared,codex-bundle,github-copilot-bundle,claude-code-bundle}.ts` 与 `src/skills/{catalog,package-layout,runtime,semver}.ts` 类型化改造并删除对应 `.js`，`scripts/examples/*.js` 继续作为 Node 入口加载 dist 产物;verify=`npm run typecheck && npm run test -- test/adapters/adapter-model.test.js test/adapters/codex-bundle.test.js test/adapters/github-copilot-bundle.test.js test/adapters/claude-code-bundle.test.js test/adapters/adapter-skill-wiring.test.js test/skills/package-layout.test.js test/skills/official-skill-assets.test.js test/skills/workspace-delivery-finisher.test.js test/ci/ci-scripts.test.js && npm run check`
  - 执行记录：review_delta=已完成 `TK-2003` 自检复核，CR 结果落盘为 `code-review/verified_review_tk-2003-migrate-adapters-skills-examples-to-ts.md`;verify=`rg --files src/adapters src/skills | sort`

- [x] **TK-2004** 迁移 `cli/runtime/commands` 到 TypeScript（负责人：Core｜优先级：P0｜截止：2026-04-01｜状态：done）
  - 执行记录：plan=迁移 CLI 主链路并补齐命令上下文与错误模型类型，保持现有命令协议稳定;result=待执行;verify=待执行
  - 执行记录：plan=先完成 `src/cli/**` 第一批迁移（command-registry/index/runtime/ui）并删除同路径 `.js`，验证命令协议兼容;result=已完成 `src/cli/{command-registry,index}.ts`、`src/cli/runtime/{context,errors,exit-codes}.ts`、`src/cli/ui/{logger,theme}.ts` 迁移并删除对应 `.js`，CLI 核心类型边界已补齐;verify=`npm run typecheck && npm run test -- test/commands/*.test.js && npm run check`
  - 执行记录：plan=继续迁移命令层与模板层文件（优先 `report/upgrade/doctor` 及共享模板），同步收敛 Biome 与 strict 类型;result=已完成 `src/commands/{automation-shared,bootstrap-shared,report-command,upgrade-command,doctor-command}.ts`、`src/commands/templates/{init-documents,plan-documents}.ts` 并删除对应 `.js`，命令层 payload/finding/summary 类型边界已补齐;verify=`npm run typecheck && npm run test -- test/commands/doctor-command.test.js test/commands/upgrade-command.test.js test/commands/init-command.test.js && npm run check`
  - 执行记录：plan=推进命令层第三批迁移，完成 `init-command` TS 化并保持初始化行为与依赖/skills 安装输出协议兼容;result=已完成 `src/commands/init-command.ts` 迁移并删除 `src/commands/init-command.js`，同时通过 Biome 与 strict typecheck 收敛迁移过渡类型;verify=`npm run typecheck && npm run test -- test/commands/init-command.test.js test/commands/doctor-command.test.js test/commands/upgrade-command.test.js test/commands/plan-command.test.js test/commands/report-command.test.js && npm run check`
  - 执行记录：plan=推进命令层第四批迁移，完成 `skills-command` TS 化并补齐 install/list/doctor 三类输出 payload 类型边界;result=已完成 `src/commands/skills-command.ts` 迁移并删除 `src/commands/skills-command.js`，`skills` 命令的 action/state/payload/finding/install operation 已显式类型化;verify=`npm run typecheck && npm run test -- test/commands/skills-command.test.js test/commands/init-command.test.js test/commands/doctor-command.test.js test/commands/upgrade-command.test.js test/skills/package-layout.test.js test/skills/official-skill-assets.test.js test/skills/workspace-delivery-finisher.test.js && npm run check`
  - 执行记录：plan=推进命令层第五批迁移，完成 `review-verify-command` TS 化并补齐 workflow 回放与 CR 生命周期输出类型边界;result=已完成 `src/commands/review-verify-command.ts` 迁移并删除 `src/commands/review-verify-command.js`，复核阶段 workflow 输出、payload 与渲染字段已显式类型化;verify=`npm run typecheck && npm run test -- test/commands/review-command.test.js test/commands/review-verify-command.test.js test/commands/skills-command.test.js test/commands/init-command.test.js test/commands/doctor-command.test.js test/commands/upgrade-command.test.js test/skills/package-layout.test.js test/skills/official-skill-assets.test.js test/skills/workspace-delivery-finisher.test.js && npm run check`
  - 执行记录：plan=推进命令层第六批迁移，完成 `check-command` TS 化并补齐 findings/stage payload/report 输出类型边界;result=已完成 `src/commands/check-command.ts` 迁移并删除 `src/commands/check-command.js`，`check` 命令的 plan/breakdown/self-check 阶段校验链路在 TS 下保持行为兼容;verify=`npm run typecheck && npm run test -- test/commands/check-command.test.js test/commands/review-verify-command.test.js test/commands/skills-command.test.js test/commands/init-command.test.js test/commands/doctor-command.test.js test/commands/upgrade-command.test.js && npm run check`
  - 执行记录：plan=推进命令层第七批迁移，完成 `plan-command` TS 化并补齐计划意图解析、workflow 产物生成与 summary 输出链路类型边界;result=已完成 `src/commands/plan-command.ts` 迁移并删除 `src/commands/plan-command.js`，计划产物生成与写入路径在 TS 下保持行为兼容;verify=`npm run typecheck && npm run test -- test/commands/plan-command.test.js test/commands/check-command.test.js test/commands/review-verify-command.test.js test/commands/skills-command.test.js test/commands/init-command.test.js test/commands/doctor-command.test.js test/commands/upgrade-command.test.js && npm run check`
  - 执行记录：plan=推进命令层第八批迁移，完成 `review-command` TS 化并补齐 target 收集、finding/summarize、workflow 与 CR 输出 payload 类型边界;result=已完成 `src/commands/review-command.ts` 迁移并删除 `src/commands/review-command.js`，`review` 命令在 TS 下保持输出协议兼容;verify=`npm run typecheck && npm run test -- test/commands/review-command.test.js test/commands/review-verify-command.test.js test/commands/check-command.test.js test/commands/plan-command.test.js && npm run check`
  - 执行记录：plan=推进命令层第九批迁移，完成剩余 `run-command` 的 `js -> ts` 收口，保证命令链路在 `src/**` 下不再保留 `.js`;result=已完成 `src/commands/run-command.ts` 迁移并删除 `src/commands/run-command.js`，`run` 命令回归通过；当前保留过渡性 `@ts-nocheck` 以避免一次性大规模类型改动风险;verify=`npm run typecheck && npm run test -- test/commands/run-command.test.js test/commands/review-command.test.js test/commands/review-verify-command.test.js && npm run check && npm run test && npm run release:ga-check`
  - 执行记录：plan=启动并完成 `run-command.ts` 类型债收敛，移除 `@ts-nocheck` 并修复剩余索引/unknown/payload 推断错误，闭环 TK-2004;result=已移除 `src/commands/run-command.ts` 的 `@ts-nocheck`，通过过渡类型与显式断言保持行为兼容，TK-2004 收口完成;verify=`npm run typecheck && npm run test -- test/commands/run-command.test.js test/commands/review-command.test.ts test/commands/review-verify-command.test.ts && npm run check && npm run test && npm run release:ga-check`

- [x] **TK-2005** 迁移测试层到 `.test.ts` 并保持 Vitest 稳定（负责人：QA｜优先级：P1｜截止：2026-04-02｜状态：done）
  - 执行记录：plan=分批把剩余 `.test.js` 迁移到 `.test.ts`，优先覆盖核心命令与发布链路;result=待执行;verify=待执行
  - 执行记录：plan=先迁移 `review/review-verify` 命令测试为 `.test.ts`，补齐测试辅助函数参数类型并保持断言逻辑不变;result=已完成 `test/commands/review-command.test.ts` 与 `test/commands/review-verify-command.test.ts` 迁移并删除对应 `.test.js`，测试行为保持兼容;verify=`npm run typecheck && npm run test -- test/commands/review-command.test.ts test/commands/review-verify-command.test.ts && npm run check`
  - 执行记录：plan=对首批迁移测试执行全量回归门禁，确认不会影响全仓库测试与发布候选验证;result=已完成全量 `npm run test` 与 `npm run release:ga-check`，验证通过;verify=`npm run test && npm run release:ga-check`
  - 执行记录：plan=继续迁移命令测试剩余 `.test.js`（automation-shared/check/doctor/init/plan/report/run/skills/upgrade）并收敛迁移后的显式类型;result=已完成 `test/commands/{automation-shared,check-command,doctor-command,init-command,plan-command,report-command,run-command,skills-command,upgrade-command}.test.ts` 并删除对应 `.test.js`，命令测试已全部切换为 `.test.ts`;verify=`npm run typecheck && npm run test -- test/commands/*.test.ts && npm run check`
  - 执行记录：plan=补跑二批迁移后的全量回归与覆盖门禁，确认测试层迁移不会破坏发布校验路径;result=已完成 `npm run test`、`npm run test:coverage` 与 `npm run release:ga-check`，最终均通过（中途出现一次 release 安装波动，复跑通过）;verify=`npm run test && npm run test:coverage && npm run release:ga-check`
  - 执行记录：plan=继续迁移非命令测试目录（acceptance/adapters/ci/config/governance/release/skills/slots/standards/workflow）到 `.test.ts` 并收敛 strict 类型;result=已完成上述目录所有 `.test.js -> .test.ts` 迁移并删除源文件，修复 `SlotDefinition`/buffer stream/unknown 相关报错，`find test -name '*.test.js'` 结果为空;verify=`npm run typecheck && npm run test -- test/acceptance/*.test.ts test/adapters/*.test.ts test/ci/*.test.ts test/config/*.test.ts test/governance/*.test.ts test/release/*.test.ts test/skills/*.test.ts test/slots/*.test.ts test/standards/*.test.ts test/workflow/*.test.ts && npm run check`
  - 执行记录：plan=修复发布链路测试并发 tarball 冲突并补跑全量回归/覆盖/发布门禁，收口 TK-2005;result=已将 `scripts/release/run-getting-started-check.sh` 与 `scripts/release/verify-local-distribution.js` 改为在临时安装目录打包，消除并发删除同名 tgz 导致的安装失败；全量验证通过，TK-2005 完成;verify=`npm run test && npm run test:coverage && npm run release:ga-check`

- [x] **TK-2006** 增加 TS-only 审计门禁并收口发布约束（负责人：Release｜优先级：P0｜截止：2026-04-03｜状态：done）
  - 执行记录：plan=新增 JS 残留白名单审计与 gate 集成，确保新增改造默认 TS 并持续通过发布门禁;result=待执行;verify=待执行
  - 执行记录：plan=新增 TS-only 审计脚本与白名单机制，默认审计 `src/**` 与 `test/**` 并支持显式 allowList;result=已完成 `scripts/governance/check-ts-only-residue.js` 与 `scripts/governance/ts-only-whitelist.json`，可识别新增 `.js` 残留并输出违规明细;verify=`node ./scripts/governance/check-ts-only-residue.js --format=json`
  - 执行记录：plan=将 TS-only 审计接入 check/release 门禁并补充回归测试，闭环 TK-2006;result=已新增 `check:ts-only` 脚本并将 `node ./scripts/governance/check-ts-only-residue.js` 接入 `code_standards.md` 验证命令，`release` 检查项新增 `npm run check:ts-only`，同时补充 `test/governance/ts-only-gate.test.ts` 与 `test/release/release-distribution.test.ts` 验证，TK-2006 完成;verify=`npm run typecheck && npm run check && npm run test && npm run test:coverage && npm run release:ga-check`
