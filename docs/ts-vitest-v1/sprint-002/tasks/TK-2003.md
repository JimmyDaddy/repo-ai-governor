# TK-2003 迁移 adapters skills 与 examples 脚本到 TypeScript

- Status: done
- Priority: P1
- Project: `ts-vitest-v1`
- Sprint: `sprint-002`
- Owner: Core
- Due: 2026-03-31
- Depends On: `TK-2002`

## Goal

完成 adapter 与 skills 生态模块迁移，保持多适配器渲染输出与安装链路行为稳定。

## Deliverables

1. `adapters/skills` 模块迁移到 `.ts`
2. `scripts/examples` 渲染脚本迁移到 TS 或统一加载 TS 构建产物
3. 补齐适配器相关回归验证

## Acceptance

1. 适配器与技能模块迁移后测试通过
2. examples 脚本输出与迁移前一致
3. `npm run check` 与关键适配器测试通过

## Progress

1. 已完成 `src/adapters/*` 与 `src/skills/*` 主体迁移到 `.ts`，并删除同路径冗余 `.js`（`src/adapters/{adapter-model,bundle-shared,codex-bundle,github-copilot-bundle,claude-code-bundle}.js` 与 `src/skills/{catalog,package-layout,runtime,semver}.js`）。
2. 已补齐 `adapters/skills` 关键类型：适配器 bundle 组装输入、slotDefinitions 与 runtime 配置桥接、skill manifest/catalog 结构类型、安装目标与 semver 匹配类型约束。
3. examples 渲染脚本保持 `.js` 执行入口（`node ./scripts/examples/*.js`）不变，继续通过 `load-dist-module.js` 加载构建产物，输出行为与迁移前一致。
4. 验证通过：`npm run typecheck`、`npm run test -- test/adapters/adapter-model.test.js test/adapters/codex-bundle.test.js test/adapters/github-copilot-bundle.test.js test/adapters/claude-code-bundle.test.js test/adapters/adapter-skill-wiring.test.js test/skills/package-layout.test.js test/skills/official-skill-assets.test.js test/skills/workspace-delivery-finisher.test.js test/ci/ci-scripts.test.js`、`npm run check`。
5. 已完成自检复核，CR 记录为 `code-review/verified_review_tk-2003-migrate-adapters-skills-examples-to-ts.md`。
