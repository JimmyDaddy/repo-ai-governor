# TK-2002 迁移 workflow slots standards config 到 TypeScript

- Status: done
- Priority: P0
- Project: `ts-vitest-v1`
- Sprint: `sprint-002`
- Owner: Core
- Due: 2026-03-30
- Depends On: `TK-2001`

## Goal

完成核心引擎与配置读取链路的 TypeScript 迁移，形成稳定可复用的核心域类型模型。

## Deliverables

1. `workflow/slots/standards/config` 模块迁移到 `.ts`
2. 保持 NodeNext ESM 导入与构建兼容
3. 同步更新相关测试与脚本引用

## Acceptance

1. 核心模块以 `.ts` 为主且构建通过
2. 相关测试在 Vitest 下通过
3. `npm run check` 通过

## Progress

1. 已完成核心模块迁移并删除同路径 `.js`：`src/workflow/*`、`src/slots/*`、`src/standards/*`、`src/config/{errors,repository-layout,load-config}`。
2. 已补齐核心域类型：工作流执行上下文/结果、slot 规则与冲突解析、standards 规则视图、配置加载输入与层合并结构。
3. 验证通过：`npm run typecheck`、`npm run test -- test/workflow/template-model.test.js test/workflow/governance-engine.test.js test/slots/slot-model.test.js test/slots/runtime.test.js test/standards/package-model.test.js test/standards/official-base-package.test.js test/config/repository-layout.test.js test/config/load-config.test.js`、`npm run check`。
4. 已完成自检复核，CR 记录为 `code-review/verified_review_tk-2002-migrate-core-workflow-slots-standards-config-to-ts.md`。
