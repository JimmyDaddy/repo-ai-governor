# TK-1005 接入 Biome formatter 与格式化命令基线

- Status: done
- Priority: P0
- Project: `ts-vitest-v1`
- Sprint: `sprint-001`
- Owner: Core
- Due: 2026-03-20
- Depends On: `none`

## Goal

在当前仓库落地 Biome formatter 基线，作为 TS 重构期与后续开发期的统一代码格式化工具。

## Deliverables

1. 根目录 `biome.json` 配置文件
2. `@biomejs/biome` 开发依赖
3. `npm run format` / `npm run format:check` 标准命令

## Acceptance

1. 可通过 `npm run format` 对代码执行格式化
2. 可通过 `npm run format:check` 执行格式化校验
3. Biome 在本阶段以 formatter-only 模式运行，不影响现有 lint 流程

## Notes

1. 后续可在 `TK-1004` 或下一 sprint 中把 `format:check` 纳入 CI/Gate。
