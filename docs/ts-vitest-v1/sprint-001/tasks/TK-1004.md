# TK-1004 对齐 CI/Gate 与发布入口的 TS/Vitest 约束

- Status: done
- Priority: P1
- Project: `ts-vitest-v1`
- Sprint: `sprint-001`
- Owner: Release
- Due: 2026-03-26
- Depends On: `TK-1001`, `TK-1002`, `TK-1003`

## Goal

确保仓库 CI、治理门禁与发布路径全部兼容 TypeScript + Vitest 基线。

## Deliverables

1. CI 流程中的 typecheck/build/test 顺序与命令更新
2. 质量门禁脚本对齐（如 check / release gate）
3. `npm pack` 本地验证流程更新

## Acceptance

1. CI 可稳定执行 `typecheck + test + check`
2. 发布前本地验收不依赖源码 JS 直接执行
3. 迁移路径在文档中可追踪

## Notes

1. 已新增 `npm run ci:quality`，并将 `.github/workflows/quality-gate.yml` 对齐到该统一入口。
2. `npm run check` 现先执行 `npm run build`，`code_standards.md` 中 CLI 校验改为 `node ./dist/bin/repo-ai-governor.js --help`，避免源码入口依赖。
3. 发布链路改为 `dist` 入口：`package.json#bin` 指向 `./dist/bin/repo-ai-governor.js`，`release` 校验脚本与本地分发验收脚本均新增 `dist` 入口断言。
4. `npm run release:ga-check`（含 `ci:quality + release:check + release:verify-local`）已验证通过。
