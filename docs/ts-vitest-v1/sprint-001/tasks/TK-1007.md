# TK-1007 清理试点双轨 JS 残留并收敛 TS-only 入口

- Status: in-progress
- Priority: P1
- Project: `ts-vitest-v1`
- Sprint: `sprint-001`
- Owner: Core
- Due: 2026-03-28
- Depends On: `TK-1003`, `TK-1004`

## Goal

收敛试点目录中 `.js/.ts` 双轨残留，建立“源码默认 TS + 运行默认 dist”的稳定路径，为后续批次迁移清障。

## Deliverables

1. 双轨文件清单与迁移顺序（含风险说明）
2. 本地与发布验收入口对齐到 `dist` 的执行策略
3. 试点目录冗余 `.js` 清理批次（`utils/config/schema/reporting`）

## Acceptance

1. 试点目录双轨文件数量显著下降（保留项需有明确理由）
2. `npm run check` 与 `npm run release:ga-check` 持续通过
3. 迁移记录在任务台账与 CR 文档可追踪

## Initial Inventory

当前检测到 5 组双轨文件（同路径同时存在 `.js` 与 `.ts`）：

1. `src/utils/common`
2. `src/config/schema/index`
3. `src/config/schema/validator`
4. `src/reporting/report-model`
5. `src/reporting/report-source`

## Notes

1. `TK-1004` 已将发布与 CI 入口对齐到 `dist`，为本任务清理源码双轨提供了前置条件。
2. 仍有部分本地脚本直接调用 `bin/repo-ai-governor.js`，本任务将先处理入口收敛，再执行 `.js` 清理，避免行为回归。

## Progress

1. 已完成入口收敛第一步：`bin/repo-ai-governor.js` 改为“优先加载 `dist/src/cli/index.js`，缺失时回退 `src/cli/index.js`”。
2. 已完成基础验证：`npm run test -- test/ci/ci-scripts.test.js test/ci/quality-gate-workflow.test.js` 与 `npm run check` 通过。
