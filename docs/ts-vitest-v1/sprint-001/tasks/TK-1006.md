# TK-1006 参考 `camera_point` 启用 Biome linter 规则与 lint 命令

- Status: done
- Priority: P0
- Project: `ts-vitest-v1`
- Sprint: `sprint-001`
- Owner: Core
- Due: 2026-03-20
- Depends On: `TK-1005`

## Goal

基于 `/Users/jimmydaddy/study/camera_point` 的 Biome 实践，给当前仓库补齐 Biome linter 能力并提供统一 `lint` 命令。

## Deliverables

1. 参考仓库模型的 `biome.json`（含 `organizeImports` 与 `linter.rules`）
2. `@biomejs/biome` 版本对齐到 `^1.9.4`
3. `npm run lint` 命令（`biome check .`）

## Acceptance

1. `npx biome --version` 为 `1.9.x`
2. `npx biome check biome.json package.json` 可执行并通过
3. `npm run lint` 命令可执行并产出 Biome 诊断结果

## Notes

1. 当前仓库历史代码存在部分 Biome 诊断，后续在 `TK-1003/TK-1004` 分批治理。
