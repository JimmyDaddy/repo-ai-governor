# TK-1002 接入 Vitest 并迁移测试运行基线

- Status: done
- Priority: P0
- Project: `ts-vitest-v1`
- Sprint: `sprint-001`
- Owner: QA
- Due: 2026-03-23
- Depends On: `TK-1001`

## Goal

将测试基线从 `node:test` 切换到 Vitest，形成统一的单测执行入口与覆盖率能力。

## Deliverables

1. `vitest.config.ts`
2. Vitest 依赖与覆盖率配置
3. `npm test` 切换到 `vitest run`
4. 首批测试迁移样例

## Acceptance

1. `npm test` 默认走 Vitest
2. 关键命令相关测试可在 Vitest 下稳定通过
3. 覆盖率可生成并可在 CI 中消费

## Notes

1. 本次迁移先保持测试文件扩展名为 `.test.js`，重点完成 runner 迁移与执行语义对齐；`.test.ts` 迁移会在后续批次推进。
2. 为兼容 Vitest，`code_standards.md` 的测试门禁参数已从 `--test-concurrency=1` 调整为 `--maxWorkers=1 --maxConcurrency=1`。
