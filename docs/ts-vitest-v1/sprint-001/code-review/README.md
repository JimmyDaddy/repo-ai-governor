# TS Vitest V1 Sprint 001 Code Review

本目录用于存放 `ts-vitest-v1 / sprint-001` 的 CR 记录。

命名规则：

1. `review_<slug>.md`
2. `verified_review_<slug>.md`
3. `resolved_review_<slug>.md`

说明：

1. `<slug>` 应包含任务 ID 或变更范围。
2. 复核结论应追加到同一份 CR 文件，然后再推进状态前缀。
