# TK-107 外置 `AGENTS.md` 当前上下文文件

- Status: done
- Priority: P0
- Project: `mvp`
- Sprint: `sprint-006`
- Owner: CLI
- Due: 2026-04-22
- Depends On: `TK-104`, `TK-105`

## Goal

把 `AGENTS.md` 中高频变化的 `Current Context` 从入口文件内联内容改成独立依赖文件，降低入口文件改动频率，并为未来多任务并发保留上下文结构。

## Deliverables

1. 独立上下文文件路径约定
2. `agentEntry.contextFile` 配置字段
3. `init` 生成独立上下文文件
4. `doctor` 校验独立上下文文件
5. 当前仓库 `AGENTS.md` 入口改造

## Acceptance

1. `AGENTS.md` 不再内联当前 project/sprint 路径
2. 当前上下文信息写入独立文件并可被 `init` 生成
3. `doctor` 能识别缺失的上下文文件
4. 结构支持后续追加并发 stream

## Notes

1. 本任务对应 `docs/mvp-issue-backlog.md` 中的 `TK-107`。
2. 该结构变更需要同时更新模板、schema、测试和当前仓库文档，避免只改一半。
3. 独立上下文文件默认路径为 `.repo-ai-governor/context/current-context.md`。
