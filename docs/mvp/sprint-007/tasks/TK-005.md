# TK-005 实现 `upgrade` 命令最小版本

- Status: done
- Priority: P0
- Project: `mvp`
- Sprint: `sprint-007`
- Owner: CLI
- Due: 2026-05-06
- Depends On: `TK-002`, `TK-103`

## Goal

补齐 `upgrade` 命令的最小能力，让 CLI 可以对模板和配置结构执行可预览、可备份的升级流程。

## Deliverables

1. `upgrade` 命令入口
2. `preview` / `backup` 语义
3. 升级结果摘要

## Acceptance

1. 支持 `--to-version`
2. 支持 `--preview`
3. 支持 `--backup`
4. 非交互模式行为稳定

## Notes

1. 本任务属于 sprint-007 Wave A。
2. CLI 设计基线见 [../../../cli-command-design.md](../../../cli-command-design.md)。
3. 需要优先保证升级流程可解释、可回滚，不要先追求复杂迁移矩阵。
4. 实现摘要见 [../upgrade-command-runtime.md](../upgrade-command-runtime.md)。
