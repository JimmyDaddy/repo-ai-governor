# Release GA Sprint 001 Checklist

- [x] **TK-701** 建立正式发布流程与版本策略（负责人：Release｜优先级：P0｜截止：2026-05-19｜状态：done）
  - 执行记录：plan=补齐正式发布流程与版本策略说明，并把 changelog 与 GA 关键校验项接入现有 release check，避免流程只停留在文档层;result=已新增 `CHANGELOG.md` 与 `ga-release-flow.md`，补齐 `release:ga-check`，并让 `check-release-ready.js` 校验 semver 版本、publish access、CHANGELOG 与 GA 必要检查项;verify=`PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/release-distribution.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run release:check`
  - 执行记录：review_delta=已完成 `TK-701` 自检复核，CR 结果落盘为 `code-review/verified_review_tk-701-establish-ga-release-flow.md`;verify=复核确认版本策略、GA 门禁、CHANGELOG 模板和脚本校验项已经对齐
  - 执行记录：plan=补齐 `CHANGELOG` 中文版，并把双语发布文档纳入 GA release readiness 校验;result=已新增 `CHANGELOG.zh-CN.md`，更新 `ga-release-flow.md`，并让 `check-release-ready.js` 同时校验中英文 `CHANGELOG` 与 `README`;verify=`PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run release:check && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/release-distribution.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run check`
  - 执行记录：review_delta=已完成 `TK-701 / TK-702` 双语发布文档与 sprint 收口复核，CR 结果落盘为 `code-review/verified_review_tk-701-tk-702-bilingual-release-docs.md`;verify=复核确认双语 changelog、双语 README、release 门禁与 sprint 收口状态已经对齐
- [x] **TK-702** 编写 README / Quick Start / 示例上手文档（负责人：Docs｜优先级：P0｜截止：2026-05-20｜状态：done）
  - 执行记录：plan=补齐仓库根目录 README、稳定的 Quick Start 和更完整的 getting started 示例，同时把 README 纳入 release readiness 检查;result=已新增根目录 `README.md`、`docs/quick-start.md`、`docs/getting-started-example.md`，覆盖 `init`、`doctor`、`plan`、`check`、`review`、`report` 的最小可运行路径，并让 `release:check` 强制校验 `README.md`;verify=`PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run release:check && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/release-distribution.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run check`
  - 执行记录：review_delta=已完成 `TK-702` 自检复核，CR 结果落盘为 `code-review/verified_review_tk-702-author-readme-and-quick-start.md`;verify=复核确认 README、Quick Start、示例上手路径与当前 CLI 能力对齐，未夸大未实现功能
  - 执行记录：plan=补齐 `README` 中文版并建立中英文互链，确保对外入口具备双语可读性;result=已新增 `README.zh-CN.md`，并在 `README.md` / `CHANGELOG.md` 中补齐语言切换入口和双语发布说明;verify=`PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run release:check && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/release-distribution.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run check`
  - 执行记录：review_delta=已完成 `TK-701 / TK-702` 双语发布文档与 sprint 收口复核，CR 结果落盘为 `code-review/verified_review_tk-701-tk-702-bilingual-release-docs.md`;verify=复核确认双语 README、双语 changelog、release 门禁与 sprint 收口状态已经对齐
- [x] **TK-703** 建立远端 release / tag / changelog 自动化骨架（负责人：ReleaseOps｜优先级：P1｜截止：2026-05-21｜状态：done）
  - 执行记录：plan=补齐远端 release workflow 骨架、release notes 渲染脚本、tag/changelog 约定和 secrets 前置条件说明，并把骨架纳入本地 release readiness 校验;result=已新增 `.github/workflows/release-ga.yml`、`scripts/release/render-release-notes.js` 和 `remote-release-automation.md`，让 `release:check` 强制校验远端 workflow 与 release notes 脚本存在;verify=`PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/release-automation.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run release:check && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run check`
  - 执行记录：review_delta=已完成 `TK-703` 自检复核，CR 结果落盘为 `code-review/verified_review_tk-703-build-remote-release-automation.md`;verify=复核确认 workflow 输入、tag/release/publish 条件化步骤、NPM_TOKEN 前置条件和 release notes 渲染链路已经对齐
- [x] **TK-704** 构建 10 分钟上手验收路径（负责人：DX｜优先级：P1｜截止：2026-05-22｜状态：done）
  - 执行记录：plan=把当前已有的安装初始化规划检查与报告能力整理成一条 10 分钟可跑通的路径，并沉淀成可复用的示例资产与回归脚本;result=已新增 `examples/release-ga-getting-started/`、`scripts/release/run-getting-started-check.sh` 和 `ten-minute-getting-started.md`，从本地 tarball 安装 CLI 后真实跑通 `init`、`doctor`、`plan`、`check`、`report` 链路，并将该脚本纳入 `release:check`;verify=`PATH=/opt/homebrew/bin:$PATH /bin/bash ./scripts/release/run-getting-started-check.sh --format=json --keep-artifacts && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/node --test test/release/getting-started-acceptance.test.js && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run release:check && PATH=/opt/homebrew/bin:$PATH /opt/homebrew/bin/npm run check`
  - 执行记录：review_delta=已完成 `TK-704` 自检复核，确认 10 分钟上手文档、示例资产、安装脚本与实际 CLI 能力一致，CR 结果落盘为 `code-review/verified_review_tk-704-build-ten-minute-getting-started.md`;verify=复核确认真实链路覆盖本地打包安装、最小治理闭环与报告生成，且 shell 路径兼容当前环境
