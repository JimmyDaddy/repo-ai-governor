{
  "name": "@repo-ai-governor/core-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node ../../scripts/build/copy-runtime-assets.js --package core-memory",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../../ --config vitest.packages.config.ts packages/core-memory/test"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/src/index.js"
    }
  },
  "dependencies": {
    "@repo-ai-governor/memory-store-adapter": "workspace:*"
  }
}
