/**
 * Defines the only public root-package export that clean-room adopters and generated host assets
 * may reference when they need the packaged service-host surface.
 */
export const PUBLIC_SERVICE_HOST_PACKAGE_EXPORT = 'repo-ai-governor/service-host';
