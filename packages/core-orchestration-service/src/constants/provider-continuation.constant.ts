/**
 * Defines the shared-session context key that stores lane-scoped provider continuations.
 */
export const SESSION_PROVIDER_CONTINUATIONS_CONTEXT_KEY = 'providerContinuations';

/**
 * Defines the current schema version for persisted provider continuation session state.
 */
export const SESSION_PROVIDER_CONTINUATIONS_VERSION = 1;
