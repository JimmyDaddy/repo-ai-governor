/**
 * Defines default replay explain limit for token-safe baseline outputs.
 */
export const DEFAULT_REPLAY_EXPLAIN_LIMIT = 20;

/**
 * Defines hard upper bound for replay explain result size.
 */
export const MAX_REPLAY_EXPLAIN_LIMIT = 500;

/**
 * Defines fallback line when replay query resolves no records.
 */
export const NO_REPLAY_MATCH_EXPLAIN_LINE = "No replay pointers matched the query filters.";
